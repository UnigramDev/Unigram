<UserControl
    x:Class="Unigram.Controls.Chats.ChatActionBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Controls.Chats"
    xmlns:controls="using:Unigram.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="ActionBar"
          Background="{ThemeResource PageSubHeaderBackgroundBrush}"
          Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="48" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="LayoutRoot">

        </Grid>

        <controls:GlyphButton Command="{x:Bind ViewModel.RemoveActionBarCommand}"
                              Glyph="&#xE711;"
                              Height="32"
                              Grid.Column="1" />
    </Grid>
</UserControl>
