<ContentDialog
    x:Class="Unigram.Controls.Views.ProxyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Controls.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Proxy Settings"
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="FieldServer" Header="Hostname" Margin="0,16,0,12"/>
        <TextBox x:Name="FieldPort" Header="Port" Margin="0,0,0,12" Grid.Row="1"/>
        <TextBox x:Name="FieldUsername" Header="Username" Margin="0,12,0,12" Grid.Row="2"/>
        <PasswordBox x:Name="FieldPassword" Header="Password" Margin="0,0,0,12" Grid.Row="3"/>
    </Grid>
</ContentDialog>
