<ContentDialog
    x:Class="Unigram.Controls.Views.CreatePollView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Controls.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TITLE"
    PrimaryButtonText="Button1"
    SecondaryButtonText="Button2"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel>
        <TextBlock Text="{CustomResource Question}" Margin="12,8,12,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>

        <TextBox PlaceholderText="{CustomResource QuestionHint}"/>

        <TextBlock Text="{CustomResource PollOptions}" Margin="12,8,12,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>

        <ItemsControl/>

        <TextBlock Text=""
                   Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Margin="12,8,12,0"/>

    </StackPanel>
</ContentDialog>
