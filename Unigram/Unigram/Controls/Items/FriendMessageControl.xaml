<UserControl
    x:Class="Unigram.Controls.Items.FriendMessageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Controls.Items"
    xmlns:vmc="using:Telegram.Api.TL"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid HorizontalAlignment="Left">
        <Border Background="{StaticResource TelegramBackgroundMessageBubbleBrush}"
                                    Margin="8"
                                    MinHeight="36"
                                    MinWidth="200"
                                    MaxWidth="400"
                                    >
            <StackPanel Orientation="Vertical"
                                            Padding="8,6">
                <TextBlock Text="{Binding ViewModel.FromId, FallbackValue='from id'}"
                                               Visibility="Visible"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,4"
                                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                               RequestedTheme="Dark"/>
                <Image Margin="0,0,0,4"
                                           Visibility="Collapsed"
                                           Source="ms-appx:///Assets/Mockups/Images/userPhoto.jpg"/>
                <TextBlock Text="{Binding ViewModel.Message, FallbackValue='message'}"
                                               TextWrapping="Wrap"
                                               RequestedTheme="Dark"/>
                <StackPanel Orientation="Horizontal" 
                                                HorizontalAlignment="Right">
                    <FontIcon x:Name="StateIcon"
                                                  Visibility="Collapsed"
                                                  Glyph="&#xE601;" 
                                                  FontFamily="{ThemeResource TelegramThemeFontFamily}"
                                                  Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                                                  FontSize="10" 
                                                  Height="10"
                                                  Width="18"
                                                  Margin="0,0,4,0"/>
                    <TextBlock Text="{x:Bind ViewModel.Date, FallbackValue='date'}"
                                                   RequestedTheme="Dark"
                                                   Style="{ThemeResource CaptionTextBlockStyle}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
