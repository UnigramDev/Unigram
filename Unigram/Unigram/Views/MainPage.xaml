<Page
    x:Class="Unigram.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views"
    xmlns:controls="using:Unigram.Controls"
    xmlns:selectors="using:Unigram.Selectors"
    xmlns:cells="using:Unigram.Controls.Cells"
    xmlns:messages="using:Unigram.Controls.Messages"
    xmlns:extensions="using:Microsoft.Toolkit.Uwp.UI.Extensions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:triggers="using:Unigram.Triggers"
    mc:Ignorable="d"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <SuppressNavigationTransitionInfo/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <CollectionViewSource x:Name="ChatsResults" Source="{x:Bind ViewModel.Chats.Search, Mode=OneWay}" IsSourceGrouped="True"/>
        <CollectionViewSource x:Name="ContactsResults" Source="{x:Bind ViewModel.Contacts.Search, Mode=OneWay}" IsSourceGrouped="True"/>

        <Style x:Key="WorkModeButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="RootGrid">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundListLowBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="RootGrid">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundListMediumBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="UncheckedContent.Visibility" Value="Collapsed"/>
                                            <Setter Target="CheckedContent.Visibility" Value="Visible"/>
                                            <Setter Target="ContentPresenter.Opacity" Value="1"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="CheckedPointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="UncheckedContent.Visibility" Value="Collapsed"/>
                                            <Setter Target="CheckedContent.Visibility" Value="Visible"/>
                                            <Setter Target="ContentPresenter.Opacity" Value="1"/>
                                        </VisualState.Setters>
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="RootGrid">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundListLowBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="CheckedPressed">
                                        <VisualState.Setters>
                                            <Setter Target="UncheckedContent.Visibility" Value="Collapsed"/>
                                            <Setter Target="CheckedContent.Visibility" Value="Visible"/>
                                            <Setter Target="ContentPresenter.Opacity" Value="1"/>
                                        </VisualState.Setters>
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="RootGrid">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundListMediumBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <TextBlock x:Name="UncheckedContent" Text="Hide muted chats" Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Margin="12,0"/>
                            <TextBlock x:Name="CheckedContent" Text="Show all chats" Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Margin="12,0" Visibility="Collapsed"/>
                            <ContentPresenter x:Name="ContentPresenter" Opacity="0" AutomationProperties.AccessibilityView="Raw" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" ContentTemplate="{TemplateBinding ContentTemplate}" ContentTransitions="{TemplateBinding ContentTransitions}" Content="{TemplateBinding Content}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" Padding="{TemplateBinding Padding}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Grid.Column="1"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="controls:MasterDetailView">
            <Setter Property="BorderBrush" Value="{ThemeResource TelegramSeparatorBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:MasterDetailView">
                        <controls:MasterDetailPanel x:Name="AdaptivePanel" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                            <Grid x:Name="DetailPresenter" Background="{TemplateBinding Background}">
                                <!--<Border x:Name="DropShadow" Loaded="DropShadow_Loaded"/>-->
                                <controls:DialogBackgroundPresenter HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                            </Grid>
                            <ContentPresenter x:Name="MasterFrame"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              HorizontalContentAlignment="Stretch"
                                              VerticalContentAlignment="Stretch"/>

                            <UserControl Canvas.ZIndex="2">
                                <Border x:Name="Grip" extensions:Mouse.Cursor="SizeWestEast" Width="12" Background="Transparent" BorderBrush="Transparent" BorderThickness="0,0,6,0">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup>
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Target="Grip.Background" Value="{ThemeResource ScrollBarThumbBackground}"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Border>
                            </UserControl>
                        </controls:MasterDetailPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <x:Double x:Key="PaneToggleButtonSize">48</x:Double>
        <Thickness x:Key="NavigationViewToggleBorderThickness">1</Thickness>
        <Thickness x:Key="NavigationViewItemBorderThickness">2,1</Thickness>

        <Style x:Key="ImageNavigationMenuItemStyle" TargetType="controls:NavigationViewItem">
            <Setter Property="Foreground" Value="{ThemeResource NavigationViewItemForeground}" />
            <Setter Property="Background" Value="{ThemeResource NavigationViewItemBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource NavigationViewItemBorderBrush}" />
            <Setter Property="BorderThickness" Value="{StaticResource NavigationViewItemBorderThickness}" />
            <Setter Property="UseSystemFocusVisuals" Value="True" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:NavigationViewItem">
                        <Grid x:Name="LayoutRoot" Height="40" Background="{TemplateBinding Background}" Control.IsTemplateFocusTarget="True">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="PointerStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundPointerOver}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundPressed}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundSelected}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundSelected}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PointerOverSelected">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundSelectedPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundSelectedPointerOver}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PressedSelected">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundSelectedPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundSelectedPressed}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DisabledStates">
                                    <VisualStateGroup.States>
                                        <VisualState x:Name="Enabled" />
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Opacity" Value="{ThemeResource ListViewItemDisabledThemeOpacity}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup.States>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="PaneStates">
                                    <VisualStateGroup.States>
                                        <VisualState x:Name="NotClosedCompact" />
                                        <VisualState x:Name="ClosedCompact" />
                                    </VisualStateGroup.States>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="IconStates">
                                    <VisualState x:Name="IconVisible" />
                                    <VisualState x:Name="IconCollapsed">
                                        <VisualState.Setters>
                                            <Setter Target="IconBox.Visibility" Value="Collapsed" />
                                            <Setter Target="IconColumn.Width" Value="16" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Rectangle x:Name="SelectionIndicator" Width="6" Height="24" Fill="{ThemeResource NavigationViewSelectionIndicatorForeground}" Opacity="0" />
                            </Grid>
                            <Grid Height="40" HorizontalAlignment="Left">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="IconColumn" Width="48" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Viewbox x:Name="IconBox" Margin="16,12">
                                    <Ellipse Width="64" Height="64">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="{Binding Tag, RelativeSource={RelativeSource Mode=TemplatedParent}}" AlignmentY="Center" AlignmentX="Center" Stretch="UniformToFill" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Viewbox>
                                <ContentPresenter x:Name="ContentPresenter"
                                                  Grid.Column="1"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <controls:NavigationView x:Name="Navigation" ItemClick="NavigationView_ItemClick">
        <!--<controls:NavigationView.PaneHeader>
            <controls:NavigationViewHeader DataContext="{x:Bind ViewModel}"/>
        </controls:NavigationView.PaneHeader>-->
        <controls:NavigationView.MenuItems>
            <controls:NavigationViewItem
                x:Name="NavigationNewChat"
                Text="{CustomResource NewGroup}"
                Content="&#xE125;"
                Glyph="&#xE125;" />
            <controls:NavigationViewItem
                x:Name="NavigationNewSecretChat"
                Text="{CustomResource NewSecretChat}"
                Content="&#xE1F6;"
                Glyph="&#xE1F6;" />
            <controls:NavigationViewItem
                x:Name="NavigationNewChannel"
                Text="{CustomResource NewChannel}"
                Content="&#xE789;"
                Glyph="&#xE789;" />
            <controls:NavigationViewItemSeparator/>

            <controls:NavigationViewItem
                x:Name="NavigationChats"
                Text="Chats"
                IsChecked="True"
                Content="&#xE8BD;"
                Glyph="&#xE8BD;" />
            <controls:NavigationViewItem
                x:Name="NavigationContacts"
                Text="{CustomResource Contacts}"
                Content="&#xE8D4;"
                Glyph="&#xE8D4;" />
            <controls:NavigationViewItem
                x:Name="NavigationCalls"
                Text="{CustomResource Calls}"
                Content="&#xE717;"
                Glyph="&#xE717;" />
            <!--<controls:NavigationViewItem Text="{CustomResource InviteFriends}" Glyph="&#xE789;" />-->
            <controls:NavigationViewItem
                x:Name="NavigationSettings"
                Text="{CustomResource Settings}"
                Content="&#xE115;"
                Glyph="&#xE115;" />
            <!--<controls:NavigationViewItem Text="Telegram FAQ" Glyph="&#xE897;" />-->
            <controls:NavigationViewItemSeparator/>
            <controls:NavigationViewItem
                x:Name="NavigationSavedMessages"
                Text="{CustomResource SavedMessages}"
                Content="&#xE907;"
                Glyph="&#xE907;"
                FontFamily="{StaticResource TelegramThemeFontFamily}" />
            <controls:NavigationViewItem
                x:Name="NavigationNews"
                Text="News"
                Content="&#xE789;"
                Glyph="&#xE789;" />
        </controls:NavigationView.MenuItems>
        <controls:MasterDetailView x:Name="MasterDetail" ViewStateChanged="OnStateChanged" Update="OnUpdate" Grid.Row="1">
            <!--<controls:NavigationView.PaneFooter>
                 <StackPanel>
                 <controls:GlyphButton x:Name="NavigationAbout" Click="About_Click" Content="About" Glyph="&#xE946;" Style="{StaticResource NavigationGlyphButtonStyle}"/>
                 </StackPanel>
                 </controls:NavigationView.PaneFooter>-->

            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <controls:ContentPageHeader>
                    <Grid x:Name="Header">
                        <Grid x:Name="MainHeader">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="DefaultHeader" Text="{x:Bind rpMasterTitlebar.SelectedItem.(PivotItem.Header), Mode=OneWay}" VerticalAlignment="Center" Margin="60,0,0,0" Style="{StaticResource BaseTextBlockStyle}"/>

                            <StackPanel x:Name="SettingsOptions" Orientation="Horizontal" Grid.Column="1">
                                <Button Padding="16,23,16,0"
                                        Style="{StaticResource EllipsisButton}">
                                    <FontIcon x:Name="EllipsisIcon"
                                              VerticalAlignment="Center"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              FontSize="16"
                                              Glyph="&#xE10C;"
                                              Height="{ThemeResource AppBarExpandButtonCircleDiameter}" />
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem x:Name="EditNameItem"
                                                            Click="{x:Bind SettingsView.EditName_Click}"
                                                            Style="{StaticResource IconMenuFlyoutItemStyle}"
                                                            Text="{CustomResource EditName}"
                                                            Tag="&#xE104;"/>
                                            <MenuFlyoutItem x:Name="LogoutItem"
                                                            Command="{x:Bind SettingsView.ViewModel.LogoutCommand}"
                                                            Style="{StaticResource IconMenuFlyoutItemStyle}"
                                                            Text="{CustomResource LogOut}"
                                                            Tag="&#xE10A;"/>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </StackPanel>
                            <StackPanel x:Name="ChatsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphToggleButton
                                    x:Name="Lock"
                                    Visibility="{x:Bind ViewModel.Passcode.IsEnabled, Mode=OneWay}"
                                    IsChecked="{x:Bind ViewModel.Passcode.IsLocked, Mode=OneWay}"
                                    UncheckedGlyph="&#xE1F7;"
                                    CheckedGlyph="&#xE1F6;"
                                    Click="Lock_Click"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <controls:GlyphButton
                                    Glyph="&#xE11A;"
                                    Click="Search_Click"
                                    FontSize="18"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            </StackPanel>
                            <StackPanel x:Name="ContactsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphButton
                                    Glyph="&#xE109;"
                                    Click="NewContact_Click"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <controls:GlyphButton
                                    Glyph="&#xE11A;"
                                    Click="Search_Click"
                                    FontSize="18"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            </StackPanel>
                        </Grid>

                        <controls:SearchTextBox x:Name="SearchField"
                                                Padding="60,4,6,4"
                                                Background="{x:Null}"
                                                BorderBrush="{x:Null}"
                                                Style="{StaticResource SearchTextBoxStyle}"
                                                PlaceholderText="{CustomResource Search}"
                                                PreventKeyboardDisplayOnProgrammaticFocus="True"
                                                Clean="Search_LostFocus"
                                                GotFocus="Search_GotFocus"
                                                LostFocus="Search_LostFocus"
                                                TextChanged="Search_TextChanged"
                                                KeyDown="Search_KeyDown"
                                                Canvas.ZIndex="1"/>
                    </Grid>
                </controls:ContentPageHeader>

                <Grid Visibility="{x:Bind ViewModel.LiveLocation.Items.Count, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="48"/>
                    </Grid.ColumnDefinitions>

                    <Button Command="{x:Bind ViewModel.LiveLocationCommand}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="48,0" Style="{StaticResource EmptyButtonStyle}" Grid.ColumnSpan="2">
                        <TextBlock TextAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}">
                            <Run Text="{CustomResource AttachLiveLocation}" FontWeight="SemiBold"/>
                            <Run Text="{x:Bind ConvertGeoLive(ViewModel.LiveLocation.Items.Count, ViewModel.LiveLocation.Items), Mode=OneWay}"/>
                        </TextBlock>
                    </Button>

                    <controls:GlyphButton Command="{x:Bind ViewModel.StopLiveLocationCommand}" Glyph="&#xE10A;" FontSize="16" Height="32" Grid.Column="1"/>

                    <Border BorderThickness="0,0,0,1" BorderBrush="{ThemeResource TelegramSeparatorBrush}" IsHitTestVisible="False" Grid.ColumnSpan="2"/>
                </Grid>

                <Pivot x:Name="rpMasterTitlebar" SelectionChanged="Pivot_SelectionChanged" Style="{StaticResource HeaderlessPivotStyle}" Grid.Row="2">
                    <PivotItem x:Name="TabChats" Header="Chats">
                        <Grid>
                            <ListView x:Name="DialogsSearchListView"
                                      ItemsSource="{x:Bind ChatsResults.View, Mode=OneWay}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None"
                                      ItemClick="ListView_ItemClick"
                                      ContainerContentChanging="DialogsSearchListView_ContainerContentChanging"
                                      SelectionChanged="ListView_SelectionChanged">
                                <ListView.Header>
                                    <StackPanel Visibility="{x:Bind ViewModel.Chats.TopChats,Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Border Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}">
                                            <TextBlock Text="{CustomResource ChatHints}" Padding="12,0,0,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                        </Border>
                                        <ListView SelectionMode="None"
                                                  IsItemClickEnabled="True"
                                                  ItemClick="ListView_ItemClick"
                                                  ContainerContentChanging="TopChats_ContainerContentChanging"
                                                  ItemsSource="{x:Bind ViewModel.Chats.TopChats, Mode=OneWay}"
                                                  Style="{StaticResource HorizontalListViewStyle}">
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="0,12">
                                                        <Grid Width="48" Height="48" Margin="12,0">
                                                            <controls:ProfilePicture IsHitTestVisible="False"/>
                                                            <Border Background="{ThemeResource TelegramDialogUnreadBadgeBrush}"
                                                                    MinWidth="20"
                                                                    Height="20"
                                                                    CornerRadius="10"
                                                                    VerticalAlignment="Top"
                                                                    HorizontalAlignment="Right"
                                                                    Margin="0,-2,-2,0"
                                                                    Visibility="Collapsed">
                                                                <TextBlock Style="{ThemeResource CaptionTextBlockStyle}"
                                                                           Foreground="{ThemeResource TelegramDialogUnreadLabelBrush}"
                                                                           TextAlignment="Center"
                                                                           Margin="4,2,4,0"/>
                                                            </Border>
                                                        </Grid>

                                                        <TextBlock Margin="0,8,0,0" TextAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="MinWidth" Value="0"/>
                                                    <Setter Property="Padding" Value="0"/>
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                        </ListView>

                                        <Border Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}">
                                            <TextBlock Text="{CustomResource Recent}" Padding="12,0,0,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </ListView.Header>
                                <ListView.ItemTemplateSelector>
                                    <selectors:SearchTemplateSelector>
                                        <selectors:SearchTemplateSelector.ChatTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,6">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>
                                                    <controls:ProfilePicture Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                    <Grid VerticalAlignment="Bottom" HorizontalAlignment="Left" Grid.Column="1">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Bottom"/>
                                                        <Grid Margin="4,0,0,-2" Grid.Column="1">
                                                            <Path Width="10" Height="10" Stretch="Uniform" Data="F1M33,0L23,3 12.999,0 9,9 0,13 2.999,23 0,33 9,37 12.999,46 23,43 33,46 37,37 46,33 43,23 46,13 37,9z" Fill="{ThemeResource TelegramDialogVerifiedBackgroundBrush}" StrokeThickness="0" />
                                                            <Path Width="16" Height="16" Stretch="Uniform" Data="F1M33,0L23,3 12.999,0 9,9 0,13 2.999,23 0,33 9,37 12.999,46 23,43 33,46 37,37 46,33 43,23 46,13 37,9z M31.485,14.514L35.727,18.757 23.707,30.778 19.464,35.021 15.221,30.778 10.271,25.828 14.514,21.586 19.464,26.535z" Fill="{ThemeResource TelegramDialogVerifiedForegroundBrush}" StrokeThickness="0" />
                                                        </Grid>
                                                    </Grid>
                                                    <TextBlock VerticalAlignment="Top"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               TextWrapping="NoWrap"
                                                               Grid.Column="1"
                                                               Grid.Row="1"
                                                               MaxLines="1"/>
                                                </Grid>
                                            </DataTemplate>
                                        </selectors:SearchTemplateSelector.ChatTemplate>
                                        <selectors:SearchTemplateSelector.MessageTemplate>
                                            <DataTemplate>
                                                <cells:ChatCell Margin="-12,0"/>
                                            </DataTemplate>
                                        </selectors:SearchTemplateSelector.MessageTemplate>
                                    </selectors:SearchTemplateSelector>
                                </ListView.ItemTemplateSelector>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemContainerTransitions>
                                    <TransitionCollection/>
                                </ListView.ItemContainerTransitions>
                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <!--<TextBlock Text="{Binding Key}" Style="{StaticResource BaseTextBlockStyle}"/>-->
                                                <TextBlock Text="{Binding Key}" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="ListViewHeaderItem">
                                                <Setter Property="Background" Value="{ThemeResource TelegramBackgroundSubtitleBarBrush}" />
                                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                                <Setter Property="Margin" Value="0,0,0,0"/>
                                                <Setter Property="Padding" Value="12,0,12,0"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="UseSystemFocusVisuals" Value="True" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListViewHeaderItem">
                                                            <Border Visibility="{Binding Content.Count, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        Visibility="{Binding Content.Key, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}">
                                                                    <ContentPresenter x:Name="ContentPresenter"
                                                                                      Margin="{TemplateBinding Padding}"
                                                                                      Content="{TemplateBinding Content}"
                                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                      ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>
                            </ListView>
                            <Grid x:Name="DialogsPanel">
                                <controls:ScrollViewerBackground
                                    Visibility="{x:Bind (Visibility)ViewModel.Chats.IsFirstPinned, Mode=OneWay}"
                                    Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}"
                                    ScrollingHost="{x:Bind ChatsList}"
                                    VerticalAlignment="Top"/>
                                <controls:ChatListView x:Name="ChatsList"
                                                       ItemsSource="{x:Bind ViewModel.Chats.Items}"
                                                       IsItemClickEnabled="True"
                                                       SelectionMode="None"
                                                       ItemClick="ListView_ItemClick"
                                                       SelectionChanged="ListView_SelectionChanged">
                                    <ListView.ItemContainerStyleSelector>
                                        <selectors:DialogStyleSelector CacheService="{x:Bind ViewModel.CacheService}">
                                            <selectors:DialogStyleSelector.DialogStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="Padding" Value="0"/>
                                                    <Setter Property="MinHeight" Value="0"/>
                                                </Style>
                                            </selectors:DialogStyleSelector.DialogStyle>
                                            <selectors:DialogStyleSelector.PinnedStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="Background" Value="{ThemeResource TelegramBackgroundSubtitleBarBrush}" />
                                                    <Setter Property="Padding" Value="0"/>
                                                    <Setter Property="MinHeight" Value="0"/>
                                                </Style>
                                            </selectors:DialogStyleSelector.PinnedStyle>
                                        </selectors:DialogStyleSelector>
                                    </ListView.ItemContainerStyleSelector>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <cells:ChatCell ContextRequested="Dialog_ContextRequested"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.Header>
                                        <ToggleButton x:Name="WorkMode" Checked="WorkMode_Click" Unchecked="WorkMode_Click" Style="{StaticResource WorkModeButtonStyle}">
                                            <Border Background="{ThemeResource TelegramDialogUnreadBadgeMutedBrush}"
                                                    MinWidth="20"
                                                    Height="20"
                                                    CornerRadius="10"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Right">
                                                <TextBlock Text="{x:Bind ViewModel.UnreadMutedCount, Mode=OneWay}"
                                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource TelegramDialogUnreadLabelBrush}"
                                                           TextAlignment="Center"
                                                           Margin="4,2,4,0"/>
                                            </Border>
                                        </ToggleButton>
                                    </ListView.Header>
                                    <ListView.Footer>
                                        <Border Height="6"/>
                                    </ListView.Footer>
                                    <ListView.ItemContainerTransitions>
                                        <TransitionCollection/>
                                    </ListView.ItemContainerTransitions>
                                </controls:ChatListView>

                                <!--<StackPanel x:Name="Arrow" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                    --><!--Visibility="{x:Bind ViewModel.Dialog.UnreadCount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"--><!--
                                    <Border Background="{ThemeResource TelegramDialogUnreadBadgeBrush}"
                                            MinWidth="20"
                                            Height="20"
                                            CornerRadius="10"
                                            HorizontalAlignment="Center"
                                            Margin="0,6,0,-12"
                                            Canvas.ZIndex="1">
                                        <TextBlock x:Name="UnreadCount"
                                                   Text="{x:Bind ViewModel.UnreadUnmutedCount, Mode=OneWay}"
                                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TelegramDialogUnreadLabelBrush}"
                                                   TextAlignment="Center"
                                                   Margin="4,2,4,0"/>
                                    </Border>
                                    <Border Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}"
                                            BorderBrush="{ThemeResource TelegramSeparatorBrush}"
                                            BorderThickness="1"
                                            CornerRadius="24"
                                            Margin="12,6"
                                            Height="38"
                                            Width="38"
                                            Grid.ColumnSpan="2">
                                        <controls:GlyphButton FontSize="16" Width="36" Height="36" Glyph="&#xE74B;" CornerRadius="24"/>
                                    </Border>
                                </StackPanel>-->
                            </Grid>
                        </Grid>
                    </PivotItem>
                    <PivotItem Loaded="PivotItem_Loaded" Header="{CustomResource Contacts}">
                        <Grid>
                            <ListView x:Name="ContactsSearchListView"
                                      ItemsSource="{x:Bind ContactsResults.View, Mode=OneWay}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None"
                                      ItemClick="ListView_ItemClick"
                                      SelectionChanged="ListView_SelectionChanged"
                                      ContainerContentChanging="ContactsSearchListView_ContainerContentChanging">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,4,0,8">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <controls:ProfilePicture Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                            <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom" Grid.Row="0"/>
                                            <TextBlock VerticalAlignment="Top"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextWrapping="NoWrap"
                                                       Grid.Column="1"
                                                       Grid.Row="1"
                                                       MaxLines="1"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemContainerTransitions>
                                    <TransitionCollection/>
                                </ListView.ItemContainerTransitions>
                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Key}" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="ListViewHeaderItem">
                                                <Setter Property="Background" Value="{ThemeResource TelegramBackgroundSubtitleBarBrush}" />
                                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                                <Setter Property="Margin" Value="0,0,0,0"/>
                                                <Setter Property="Padding" Value="12,0,12,0"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="UseSystemFocusVisuals" Value="True" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListViewHeaderItem">
                                                            <Border Visibility="{Binding Content.Count, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        Visibility="{Binding Content.Key, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}">
                                                                    <ContentPresenter x:Name="ContentPresenter"
                                                                                      Margin="{TemplateBinding Padding}"
                                                                                      Content="{TemplateBinding Content}"
                                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                      ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>
                            </ListView>
                            <Grid x:Name="ContactsPanel">
                                <ListView x:Name="UsersListView"
                                          ItemsSource="{x:Bind ViewModel.Contacts.Items}"
                                          IsItemClickEnabled="True"
                                          ItemClick="ListView_ItemClick"
                                          ContainerContentChanging="UsersListView_ContainerContentChanging"
                                          SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,6">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition/>
                                                </Grid.ColumnDefinitions>
                                                <controls:ProfilePicture IsEnabled="False" Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom"/>
                                                <TextBlock VerticalAlignment="Top"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="NoWrap"
                                                           Grid.Column="1"
                                                           Grid.Row="1"
                                                           MaxLines="1"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <!--<ListView.ItemContainerTransitions>
                                         <TransitionCollection/>
                                         </ListView.ItemContainerTransitions>-->
                                </ListView>
                            </Grid>
                        </Grid>
                    </PivotItem>
                    <PivotItem x:Name="Calls" Header="{CustomResource Calls}">
                        <ListView ItemsSource="{x:Bind ViewModel.Calls.Items}"
                                  ItemClick="ListView_ItemClick"
                                  IsItemClickEnabled="True"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <messages:HistoryCallMessageControl ContextRequested="Call_ContextRequested"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <!--<ListView.ItemContainerTransitions>
                                 <TransitionCollection/>
                                 </ListView.ItemContainerTransitions>-->
                        </ListView>
                    </PivotItem>
                    <PivotItem Header="{CustomResource Settings}">
                        <local:SettingsPage x:Name="SettingsView" MasterDetail="{x:Bind MasterDetail}"/>
                    </PivotItem>
                </Pivot>

                <Border x:Name="Separator" BorderBrush="{ThemeResource TelegramSeparatorBrush}" IsHitTestVisible="False" HorizontalAlignment="Right" Grid.RowSpan="3" Grid.Row="1"/>
            </Grid>
        </controls:MasterDetailView>
    </controls:NavigationView>
</Page>
