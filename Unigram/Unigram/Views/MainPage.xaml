<Page
    x:Class="Unigram.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views"
    xmlns:controls="using:Unigram.Controls"
    xmlns:selectors="using:Unigram.Selectors"
    xmlns:items="using:Unigram.Controls.Items"
    xmlns:triggers="using:Unigram.Triggers"
    xmlns:models="using:Telegram.Api.TL"
    xmlns:messages="using:Unigram.Controls.Messages"
    xmlns:behaviors="using:Unigram.Behaviors"
    xmlns:vms="using:Unigram.ViewModels"
    xmlns:win2d="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition FromVerticalOffset="20"/>
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <CollectionViewSource x:Name="DialogsResults" Source="{x:Bind ViewModel.Dialogs.Search}" IsSourceGrouped="True"/>
        <CollectionViewSource x:Name="ContactsResults" Source="{x:Bind ViewModel.Contacts.Search}" IsSourceGrouped="True"/>

        <Style TargetType="PivotItem">
            <Setter Property="Margin" Value="0" />
        </Style>

        <Style TargetType="controls:MasterDetailView">
            <Setter Property="BorderBrush" Value="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
            <!--<Setter Property="Background">
                 <Setter.Value>
                 <ImageBrush ImageSource="{ThemeResource DefaultBackgroundBrush}" Stretch="UniformToFill"/>
                 </Setter.Value>
                 </Setter>-->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:MasterDetailView">
                        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="AdaptiveStates">
                                    <VisualState x:Name="FilledState">
                                        <VisualState.StateTriggers>
                                            <AdaptiveTrigger MinWindowWidth="820"/>
                                        </VisualState.StateTriggers>
                                        <VisualState.Setters>
                                            <Setter Target="MasterColumn.Width" Value="260*" />
                                            <Setter Target="DetailColumn.Width" Value="540*" />
                                            <!--<Setter Target="MasterColumn.MinWidth" Value="260" />-->
                                            <Setter Target="MasterColumn.MinWidth" Value="72" />
                                            <Setter Target="MasterColumn.MaxWidth" Value="540" />
                                            <Setter Target="DetailPresenter.(Grid.Column)" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="NarrowState">
                                        <VisualState.StateTriggers>
                                            <AdaptiveTrigger MinWindowWidth="0"/>
                                        </VisualState.StateTriggers>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="MasterColumn"/>
                                <ColumnDefinition x:Name="DetailColumn" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <Grid x:Name="DetailPresenter" Background="{TemplateBinding Background}">
                                <controls:DialogBackgroundPresenter HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                            </Grid>
                            <ContentPresenter x:Name="MasterFrame"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              HorizontalContentAlignment="Stretch"
                                              VerticalContentAlignment="Stretch"/>

                            <!--<toolkit:GridSplitter
                                Background="Transparent"
                                HorizontalAlignment="Left"
                                Grid.Column="1"
                                ResizeDirection="Auto"
                                ResizeBehavior="BasedOnAlignment"
                                CursorBehavior="ChangeOnSplitterHover"/>-->
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <x:Double x:Key="PaneToggleButtonSize">48</x:Double>
        <Thickness x:Key="NavigationViewToggleBorderThickness">1</Thickness>
        <Thickness x:Key="NavigationViewItemBorderThickness">2,1</Thickness>

        <Style x:Key="ImageNavigationMenuItemStyle" TargetType="controls:NavigationViewItem">
            <Setter Property="Foreground" Value="{ThemeResource NavigationViewItemForeground}" />
            <Setter Property="Background" Value="{ThemeResource NavigationViewItemBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource NavigationViewItemBorderBrush}" />
            <Setter Property="BorderThickness" Value="{StaticResource NavigationViewItemBorderThickness}" />
            <Setter Property="UseSystemFocusVisuals" Value="True" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:NavigationViewItem">
                        <Grid x:Name="LayoutRoot" Height="40" Background="{TemplateBinding Background}" Control.IsTemplateFocusTarget="True">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="PointerStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundPointerOver}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundPressed}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundSelected}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundSelected}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PointerOverSelected">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundSelectedPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundSelectedPointerOver}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PressedSelected">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Background" Value="{ThemeResource NavigationViewItemBackgroundSelectedPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource NavigationViewItemForegroundSelectedPressed}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DisabledStates">
                                    <VisualStateGroup.States>
                                        <VisualState x:Name="Enabled" />
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Opacity" Value="{ThemeResource ListViewItemDisabledThemeOpacity}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup.States>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="PaneStates">
                                    <VisualStateGroup.States>
                                        <VisualState x:Name="NotClosedCompact" />
                                        <VisualState x:Name="ClosedCompact" />
                                    </VisualStateGroup.States>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="IconStates">
                                    <VisualState x:Name="IconVisible" />
                                    <VisualState x:Name="IconCollapsed">
                                        <VisualState.Setters>
                                            <Setter Target="IconBox.Visibility" Value="Collapsed" />
                                            <Setter Target="IconColumn.Width" Value="16" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Rectangle x:Name="SelectionIndicator" Width="6" Height="24" Fill="{ThemeResource NavigationViewSelectionIndicatorForeground}" Opacity="0" />
                            </Grid>
                            <Grid Height="40" HorizontalAlignment="Left">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="IconColumn" Width="48" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Viewbox x:Name="IconBox" Margin="16,12">
                                    <Ellipse Width="64" Height="64">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="{Binding Tag, RelativeSource={RelativeSource Mode=TemplatedParent}}" AlignmentY="Center" AlignmentX="Center" Stretch="UniformToFill" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Viewbox>
                                <ContentPresenter x:Name="ContentPresenter"
                                              Grid.Column="1"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <controls:MasterDetailView x:Name="MasterDetail" ViewStateChanged="OnStateChanged" Update="OnUpdate" Grid.Row="1">
        <controls:NavigationView x:Name="Navigation" ItemClick="NavigationView_ItemClick">
            <controls:NavigationView.MenuItems>
                <controls:NavigationViewItem
                    x:Name="NavigationNewChat"
                    Content="{CustomResource NewGroup}"
                    Glyph="&#xE125;" />
                <!--<controls:NavigationViewItem Text="New Secret Chat" Glyph="&#xE125;" />-->
                <controls:NavigationViewItem
                    x:Name="NavigationNewChannel"
                    Content="{CustomResource NewChannel}"
                    Glyph="&#xE789;" />
                <controls:NavigationViewItemSeparator/>

                <controls:NavigationViewItem
                    x:Name="NavigationChats"
                    Content="Chats"
                    IsChecked="True"
                    Glyph="&#xE8BD;" />
                <controls:NavigationViewItem
                    x:Name="NavigationContacts"
                    Content="{CustomResource Contacts}"
                    Glyph="&#xE8D4;" />
                <controls:NavigationViewItem
                    x:Name="NavigationCalls"
                    Content="{CustomResource Calls}"
                    Glyph="&#xE717;" />
                <!--<controls:NavigationViewItem Text="{CustomResource InviteFriends}" Glyph="&#xE789;" />-->
                <controls:NavigationViewItem
                    x:Name="NavigationSettings"
                    Content="{CustomResource Settings}"
                    Glyph="&#xE115;" />
                <!--<controls:NavigationViewItem Text="Telegram FAQ" Glyph="&#xE897;" />-->
                <controls:NavigationViewItemSeparator/>
                <controls:NavigationViewItem
                    x:Name="NavigationSavedMessages"
                    Content="{CustomResource SavedMessages}"
                    Glyph="&#xE907;"
                    FontFamily="{StaticResource TelegramThemeFontFamily}" />
                <controls:NavigationViewItem
                    x:Name="NavigationNews"
                    Content="News"
                    Glyph="&#xE789;" />
            </controls:NavigationView.MenuItems>
            <controls:NavigationView.PaneFooter>
                <StackPanel>
                    <controls:GlyphButton x:Name="NavigationAbout" Click="About_Click" Content="About" Glyph="&#xE946;" Style="{StaticResource NavigationGlyphButtonStyle}"/>
                    <!--<controls:GlyphButton Click="Self_Click" Content="{x:Bind ViewModel.Contacts.Self.FullName, Mode=OneWay}" Tag="{x:Bind ViewModel.Contacts.Self.PhotoSelf, Mode=OneWay, Converter={StaticResource DefaultPhotoConverter}}" Style="{StaticResource NavigationImageGlyphButtonStyle}"/>-->
                </StackPanel>
            </controls:NavigationView.PaneFooter>

            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <controls:ContentPageHeader>
                    <Grid>
                        <Grid x:Name="MainHeader">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="DefaultHeader" Text="{x:Bind rpMasterTitlebar.SelectedItem.(PivotItem.Header), Mode=OneWay}" VerticalAlignment="Center" Margin="60,0,0,0" Style="{StaticResource BaseTextBlockStyle}"/>

                            <StackPanel x:Name="SettingsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphButton
                                    Glyph="&#xE114;"
                                    Click="EditPhoto_Click"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <Button Padding="16,23,16,0"
                                        Style="{StaticResource EllipsisButton}">
                                    <FontIcon x:Name="EllipsisIcon"
                                                VerticalAlignment="Center"
                                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                FontSize="16"
                                                Glyph="&#xE10C;"
                                                Height="{ThemeResource AppBarExpandButtonCircleDiameter}" />
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem x:Name="EditNameItem"
                                                            Click="{x:Bind SettingsView.EditName_Click}"
                                                            Style="{StaticResource IconMenuFlyoutItemStyle}"
                                                            Text="{CustomResource EditName}"
                                                            Tag="&#xE104;"/>
                                            <MenuFlyoutItem x:Name="LogoutItem"
                                                            Command="{x:Bind SettingsView.ViewModel.LogoutCommand}"
                                                            Style="{StaticResource IconMenuFlyoutItemStyle}"
                                                            Text="{CustomResource LogOut}"
                                                            Tag="&#xE10A;"/>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </StackPanel>
                            <StackPanel x:Name="ChatsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphToggleButton
                                    x:Name="Lock"
                                    Visibility="{x:Bind ViewModel.Passcode.IsEnabled, Mode=OneWay}"
                                    IsChecked="{x:Bind ViewModel.Passcode.IsLocked, Mode=OneWay}"
                                    UncheckedGlyph="&#xE1F7;"
                                    CheckedGlyph="&#xE1F6;"
                                    Click="Lock_Click"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <controls:GlyphButton
                                    Glyph="&#xE11A;"
                                    Click="Search_Click"
                                    FontSize="18"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            </StackPanel>
                            <StackPanel x:Name="ContactsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphButton
                                    Glyph="&#xE109;"
                                    Click="NewContact_Click"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <controls:GlyphButton
                                    Glyph="&#xE11A;"
                                    Click="Search_Click"
                                    FontSize="18"
                                    Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            </StackPanel>
                        </Grid>

                        <controls:SearchTextBox x:Name="SearchField"
                                                Padding="60,4,6,4"
                                                Background="{x:Null}"
                                                BorderBrush="{x:Null}"
                                                Style="{StaticResource SearchTextBoxStyle}"
                                                PlaceholderText="{CustomResource Search}"
                                                PreventKeyboardDisplayOnProgrammaticFocus="True"
                                                Clean="Search_LostFocus"
                                                LostFocus="Search_LostFocus"
                                                TextChanged="Search_TextChanged"
                                                KeyDown="Search_KeyDown"
                                                Canvas.ZIndex="1"/>
                    </Grid>
                </controls:ContentPageHeader>

                <Grid Visibility="{x:Bind ViewModel.LiveLocation.Items.Count, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="48"/>
                    </Grid.ColumnDefinitions>

                    <Button Command="{x:Bind ViewModel.LiveLocationCommand}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="48,0" Style="{StaticResource EmptyButtonStyle}" Grid.ColumnSpan="2">
                        <TextBlock TextAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}">
                            <Run Text="{CustomResource AttachLiveLocation}" FontWeight="SemiBold"/>
                            <Run Text="{x:Bind ConvertGeoLive(ViewModel.LiveLocation.Items.Count, ViewModel.LiveLocation.Items), Mode=OneWay}"/>
                        </TextBlock>
                    </Button>

                    <controls:GlyphButton Command="{x:Bind ViewModel.StopLiveLocationCommand}" Glyph="&#xE10A;" FontSize="16" Height="32" Grid.Column="1"/>

                    <Border BorderThickness="0,0,0,1" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" IsHitTestVisible="False" Grid.ColumnSpan="2"/>
                </Grid>

                <Pivot x:Name="rpMasterTitlebar" SelectionChanged="Pivot_SelectionChanged" Style="{StaticResource HeaderlessPivotStyle}" Grid.Row="2">
                    <PivotItem x:Name="TabChats" Header="Chats">
                        <Grid>
                            <ListView x:Name="DialogsSearchListView"
                                      ItemsSource="{x:Bind DialogsResults.View}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None"
                                      ItemClick="ListView_ItemClick"
                                      SelectionChanged="ListView_SelectionChanged">
                                <ListView.ItemTemplateSelector>
                                    <selectors:SearchTemplateSelector>
                                        <selectors:SearchTemplateSelector.UserTemplate>
                                            <DataTemplate x:DataType="models:TLUser">
                                                <Grid Margin="0,4,0,8">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>
                                                    <controls:ProfilePicture Source="{x:Bind PhotoSelf, Mode=OneWay, Converter={StaticResource DefaultPhotoConverter}}" Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                    <TextBlock Text="{x:Bind FullName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom" Grid.Row="0"/>
                                                    <TextBlock Text="{x:Bind Username}"
                                                               VerticalAlignment="Top"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               TextWrapping="NoWrap"
                                                               Grid.Column="1"
                                                               Grid.Row="1"
                                                               MaxLines="1">
                                                        <i:Interaction.Behaviors>
                                                            <behaviors:UsernameHighlightBehavior SearchString="{Binding ElementName=SearchDialogs, Path=DataContext.Dialogs.SearchQuery}" HighlightBrush="{ThemeResource TelegramDialogLabelFromBrush}"/>
                                                        </i:Interaction.Behaviors>
                                                    </TextBlock>
                                                </Grid>
                                            </DataTemplate>
                                        </selectors:SearchTemplateSelector.UserTemplate>
                                        <selectors:SearchTemplateSelector.ChannelTemplate>
                                            <DataTemplate x:DataType="models:TLChannel">
                                                <Grid Margin="0,6">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>
                                                    <controls:ProfilePicture Source="{x:Bind PhotoSelf, Mode=OneWay, Converter={StaticResource DefaultPhotoConverter}}" Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                    <Grid VerticalAlignment="Bottom" HorizontalAlignment="Left" Grid.Column="1">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{x:Bind DisplayName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Bottom"/>
                                                        <Grid Margin="4,0,0,-2" Visibility="{x:Bind (Visibility)IsVerified, Mode=OneWay}" Grid.Column="1">
                                                            <Path Width="10" Height="10" Stretch="Uniform" Data="F1M33,0L23,3 12.999,0 9,9 0,13 2.999,23 0,33 9,37 12.999,46 23,43 33,46 37,37 46,33 43,23 46,13 37,9z" Fill="{ThemeResource TelegramDialogVerifiedBackgroundBrush}" StrokeThickness="0" />
                                                            <Path Width="16" Height="16" Stretch="Uniform" Data="F1M33,0L23,3 12.999,0 9,9 0,13 2.999,23 0,33 9,37 12.999,46 23,43 33,46 37,37 46,33 43,23 46,13 37,9z M31.485,14.514L35.727,18.757 23.707,30.778 19.464,35.021 15.221,30.778 10.271,25.828 14.514,21.586 19.464,26.535z" Fill="{ThemeResource TelegramDialogVerifiedForegroundBrush}" StrokeThickness="0" />
                                                        </Grid>
                                                    </Grid>
                                                    <TextBlock Text="{x:Bind Converter={StaticResource ChannelToSummaryConverter}}"
                                                               VerticalAlignment="Top"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               TextWrapping="NoWrap"
                                                               Grid.Column="1"
                                                               Grid.Row="1"
                                                               MaxLines="1">
                                                        <i:Interaction.Behaviors>
                                                            <behaviors:UsernameHighlightBehavior SearchString="{Binding ElementName=SearchDialogs, Path=DataContext.Dialogs.SearchQuery}" HighlightBrush="{ThemeResource TelegramDialogLabelFromBrush}"/>
                                                        </i:Interaction.Behaviors>
                                                    </TextBlock>
                                                </Grid>
                                            </DataTemplate>
                                        </selectors:SearchTemplateSelector.ChannelTemplate>
                                        <selectors:SearchTemplateSelector.ChannelForbiddenTemplate>
                                            <DataTemplate x:DataType="models:TLChannelForbidden">
                                                <Grid Margin="0,6">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid Background="{x:Bind Id, Converter={StaticResource BubblePlaceholderConverter}}" Width="36" Height="36" CornerRadius="18" Margin="0,0,12,0" Grid.RowSpan="2">
                                                        <TextBlock Foreground="White" Text="{x:Bind Converter={StaticResource InitialNameStringConverter}}" VerticalAlignment="Center" TextAlignment="Center"/>
                                                    </Grid>
                                                    <TextBlock Text="{x:Bind DisplayName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom" Grid.Row="0"/>
                                                    <TextBlock Text="{Binding ElementName=SearchDialogs, Path=DataContext.Dialogs.SearchQuery}"
                                                               VerticalAlignment="Top"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               TextWrapping="NoWrap"
                                                               Grid.Column="1"
                                                               Grid.Row="1"
                                                               MaxLines="1">
                                                        <i:Interaction.Behaviors>
                                                            <behaviors:UsernameHighlightBehavior SearchString="{Binding ElementName=SearchDialogs, Path=DataContext.Dialogs.SearchQuery}" HighlightBrush="{ThemeResource TelegramDialogLabelFromBrush}"/>
                                                        </i:Interaction.Behaviors>
                                                    </TextBlock>
                                                </Grid>
                                            </DataTemplate>
                                        </selectors:SearchTemplateSelector.ChannelForbiddenTemplate>
                                        <selectors:SearchTemplateSelector.MessageTemplate>
                                            <DataTemplate>
                                                <items:DialogListViewItem Margin="-12,0"/>
                                            </DataTemplate>
                                        </selectors:SearchTemplateSelector.MessageTemplate>
                                    </selectors:SearchTemplateSelector>
                                </ListView.ItemTemplateSelector>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemContainerTransitions>
                                    <TransitionCollection/>
                                </ListView.ItemContainerTransitions>
                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Key}" Style="{StaticResource BaseTextBlockStyle}"/>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="ListViewHeaderItem">
                                                <Setter Property="Background" Value="{ThemeResource TelegramBackgroundSubtitleBarBrush}" />
                                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                                <Setter Property="Margin" Value="0,0,0,0"/>
                                                <Setter Property="Padding" Value="12,8,12,9"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="UseSystemFocusVisuals" Value="True" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListViewHeaderItem">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    Visibility="{Binding Content.Key, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}">
                                                                <ContentPresenter x:Name="ContentPresenter"
                                                                                  Margin="{TemplateBinding Padding}"
                                                                                  Content="{TemplateBinding Content}"
                                                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>
                            </ListView>
                            <Grid x:Name="DialogsPanel">
                                <controls:ScrollViewerBackground
                                    Visibility="{x:Bind (Visibility)ViewModel.Dialogs.IsFirstPinned, Mode=OneWay}"
                                    Background="{ThemeResource TelegramBackgroundSubtitleBarBrush}"
                                    ScrollingHost="{x:Bind DialogsListView}"
                                    VerticalAlignment="Top"/>
                                <controls:DialogListView x:Name="DialogsListView"
                                                         ItemsSource="{x:Bind ViewModel.Dialogs.Items}"
                                                         IsItemClickEnabled="True"
                                                         SelectionMode="None"
                                                         ItemClick="ListView_ItemClick"
                                                         SelectionChanged="ListView_SelectionChanged">
                                    <!--CanDragItems="True" 
                                         AllowDrop="True"-->
                                    <ListView.ItemContainerStyleSelector>
                                        <selectors:DialogStyleSelector>
                                            <selectors:DialogStyleSelector.DialogStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="Padding" Value="0"/>
                                                </Style>
                                            </selectors:DialogStyleSelector.DialogStyle>
                                            <selectors:DialogStyleSelector.PinnedStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="Background" Value="{ThemeResource TelegramBackgroundSubtitleBarBrush}" />
                                                    <Setter Property="Padding" Value="0"/>
                                                </Style>
                                            </selectors:DialogStyleSelector.PinnedStyle>
                                        </selectors:DialogStyleSelector>
                                    </ListView.ItemContainerStyleSelector>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <items:DialogListViewItem ContextRequested="Dialog_ContextRequested"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.Footer>
                                        <Border Height="6"/>
                                    </ListView.Footer>
                                    <ListView.ItemContainerTransitions>
                                        <TransitionCollection/>
                                    </ListView.ItemContainerTransitions>
                                </controls:DialogListView>
                            </Grid>
                        </Grid>
                    </PivotItem>
                    <PivotItem Loaded="PivotItem_Loaded" Header="{CustomResource Contacts}">
                        <Grid>
                            <ListView x:Name="ContactsSearchListView"
                                      ItemsSource="{x:Bind ContactsResults.View}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None"
                                      ItemClick="ListView_ItemClick"
                                      SelectionChanged="ListView_SelectionChanged">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:TLUser">
                                        <Grid Margin="0,4,0,8">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <controls:ProfilePicture Source="{x:Bind PhotoSelf, Mode=OneWay, Converter={StaticResource DefaultPhotoConverter}}" Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                            <TextBlock Text="{x:Bind FullName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom" Grid.Row="0"/>
                                            <TextBlock Text="{x:Bind Username}"
                                                       VerticalAlignment="Top"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextWrapping="NoWrap"
                                                       Grid.Column="1"
                                                       Grid.Row="1"
                                                       MaxLines="1">
                                                <i:Interaction.Behaviors>
                                                    <behaviors:UsernameHighlightBehavior SearchString="{Binding ElementName=SearchDialogs, Path=DataContext.Dialogs.SearchQuery}" HighlightBrush="{ThemeResource TelegramDialogLabelFromBrush}"/>
                                                </i:Interaction.Behaviors>
                                            </TextBlock>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemContainerTransitions>
                                    <TransitionCollection/>
                                </ListView.ItemContainerTransitions>
                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Key}" Style="{StaticResource BaseTextBlockStyle}"/>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="ListViewHeaderItem">
                                                <Setter Property="Background" Value="{ThemeResource TelegramBackgroundSubtitleBarBrush}" />
                                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                                <Setter Property="Margin" Value="0,0,0,0"/>
                                                <Setter Property="Padding" Value="12,8,12,9"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="UseSystemFocusVisuals" Value="True" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListViewHeaderItem">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    Visibility="{Binding Content.Key, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}">
                                                                <ContentPresenter x:Name="ContentPresenter"
                                                                                  Margin="{TemplateBinding Padding}"
                                                                                  Content="{TemplateBinding Content}"
                                                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>
                            </ListView>
                            <Grid x:Name="ContactsPanel">
                                <ListView x:Name="UsersListView"
                                          ItemsSource="{x:Bind ViewModel.Contacts.Items}"
                                          IsItemClickEnabled="True"
                                          ItemClick="ListView_ItemClick"
                                          SelectionChanged="ListView_SelectionChanged">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:TLUser">
                                            <Grid Margin="0,6">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition/>
                                                </Grid.ColumnDefinitions>
                                                <controls:ProfilePicture x:Phase="3" Source="{x:Bind PhotoSelf, Mode=OneWay, Converter={StaticResource DefaultPhotoConverter}}" IsEnabled="False" Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                <TextBlock Text="{x:Bind FullName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom" Grid.Row="0"/>
                                                <TextBlock Text="{x:Bind Converter={StaticResource LastSeenConverter}, ConverterParameter=list}"
                                                           x:Phase="1"
                                                           VerticalAlignment="Top"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="NoWrap"
                                                           Grid.Column="1"
                                                           Grid.Row="1"
                                                           MaxLines="1"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <!--<ListView.ItemContainerTransitions>
                                        <TransitionCollection/>
                                    </ListView.ItemContainerTransitions>-->
                                </ListView>
                            </Grid>
                        </Grid>
                    </PivotItem>
                    <PivotItem x:Name="Calls" Header="{CustomResource Calls}">
                        <ListView ItemsSource="{x:Bind ViewModel.Calls.Items}"
                                  ItemClick="ListView_ItemClick"
                                  IsItemClickEnabled="True"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <messages:HistoryCallMessageControl ContextRequested="Call_ContextRequested"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <!--<ListView.ItemContainerTransitions>
                                <TransitionCollection/>
                            </ListView.ItemContainerTransitions>-->
                        </ListView>
                    </PivotItem>
                    <PivotItem Header="{CustomResource Settings}">
                        <local:SettingsPage x:Name="SettingsView" MasterDetail="{x:Bind MasterDetail}"/>
                    </PivotItem>
                </Pivot>

                <Border x:Name="Separator" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" IsHitTestVisible="False" HorizontalAlignment="Right" Grid.RowSpan="3" Grid.Row="1"/>
            </Grid>
        </controls:NavigationView>
    </controls:MasterDetailView>
</Page>
