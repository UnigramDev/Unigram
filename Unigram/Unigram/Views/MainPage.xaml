<Page
    x:Class="Unigram.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:contract6Present="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract,6)"
    xmlns:contract7Present="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract,7)"
    xmlns:local="using:Unigram.Views"
    xmlns:common="using:Unigram.Common"
    xmlns:controls="using:Unigram.Controls"
    xmlns:collections="using:Unigram.Collections"
    xmlns:selectors="using:Unigram.Selectors"
    xmlns:cells="using:Unigram.Controls.Cells"
    xmlns:chats="using:Unigram.Controls.Chats"
    xmlns:viewModels="using:Unigram.ViewModels"
    xmlns:messages="using:Unigram.Controls.Messages"
    xmlns:microsoft="using:Microsoft.UI.Xaml.Controls"
    xmlns:td="using:Telegram.Td.Api"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:triggers="using:Unigram.Triggers"
    mc:Ignorable="d"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <SuppressNavigationTransitionInfo/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <CollectionViewSource x:Name="ChatsResults" Source="{x:Bind ViewModel.Chats.Search, Mode=OneWay}" IsSourceGrouped="True"/>
        <CollectionViewSource x:Name="ContactsResults" Source="{x:Bind ViewModel.Contacts.Search, Mode=OneWay}" IsSourceGrouped="True"/>

        <Style TargetType="controls:MasterDetailView">
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:MasterDetailView">
                        <controls:MasterDetailPanel x:Name="AdaptivePanel" IsBlank="{TemplateBinding IsBlank}" Background="{ThemeResource PageBackgroundDarkBrush}">
                            <chats:ChatBackgroundPresenter
                                Background="{ThemeResource ChatPageBackgroundBrush}"
                                Opacity="{TemplateBinding BackgroundOpacity}"
                                Loading="ChatBackgroundPresenter_Loading"/>

                            <!--<Grid BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding Padding}" VerticalAlignment="Top"/>-->

                            <ContentControl Content="{TemplateBinding MasterHeader}" IsTabStop="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" VerticalAlignment="Top"/>
                            <ContentControl Content="{TemplateBinding DetailHeader}" IsTabStop="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" VerticalAlignment="Top"/>
                            <ContentControl Content="{TemplateBinding Banner}" IsTabStop="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" VerticalAlignment="Top"/>

                            <Grid x:Name="DetailPresenter">
                                <Border VerticalAlignment="Top" Background="{TemplateBinding BorderBrush}" Height="{ThemeResource AppBarThemeCompactHeight}"/>
                            </Grid>

                            <ContentPresenter x:Name="MasterFrame"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              HorizontalContentAlignment="Stretch"
                                              VerticalContentAlignment="Stretch"/>

                            <UserControl Canvas.ZIndex="2">
                                <Border x:Name="Grip" Width="12" Background="Transparent" BorderBrush="Transparent" BorderThickness="0,0,6,0">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup>
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Target="Grip.Background" Value="{ThemeResource ScrollBarThumbBackground}"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Border>
                            </UserControl>
                        </controls:MasterDetailPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="TitleBarrr" Height="32" Background="{ThemeResource PageTitleBackgroundBrush}" Grid.ColumnSpan="2">
            <contract7Present:Grid.BackgroundTransition>
                <contract7Present:BrushTransition/>
            </contract7Present:Grid.BackgroundTransition>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <microsoft:ProgressBar x:Name="Status" VerticalAlignment="Top" Background="Transparent" Grid.ColumnSpan="2"/>
            <TextBlock x:Name="StatusLabel"
                               VerticalAlignment="Center"
                               TextLineBounds="TrimToCapHeight"
                               Margin="6,0,0,0"
                               AutomationProperties.LiveSetting="Assertive"
                               Foreground="{ThemeResource PageHeaderForegroundBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Grid.Column="1"/>
        </Grid>

        <Border
            x:Load="False"
            x:Name="ChatTabsLeft"
            Background="{ThemeResource SystemControlAcrylicWindowMediumHighBrush}"
            BorderBrush="{ThemeResource PageHeaderBackgroundBrush}"
            BorderThickness="0,0,0,0"
            Grid.Row="1">
            <controls:TopNavView
                x:Name="ChatFiltersSide"
                ItemsSource="{x:Bind ViewModel.Filters}"
                SelectedItem="{x:Bind ConvertFilter(ViewModel.SelectedFilter), Mode=TwoWay, BindBack=ConvertFilterBack}"
                IsItemClickEnabled="True"
                ItemClick="ChatFilter_ItemClick"
                ItemContextRequested="ChatFilter_ContextRequested"
                Orientation="Vertical"
                AllowDrop="True"
                CanDragItems="True"
                CanReorderItems="False"
                DragItemsStarting="ChatFilters_DragItemsStarting"
                DragItemsCompleted="ChatFilters_DragItemsCompleted"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.HorizontalScrollMode="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollMode="Auto"
                Height="Auto"
                Width="72">
                <controls:TopNavView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:ChatFilterViewModel">
                        <StackPanel Orientation="Vertical">
                            <Grid>
                                <BitmapIcon
                                    UriSource="{x:Bind IconUri, Mode=OneWay}"
                                    HorizontalAlignment="Center"
                                    Width="36"
                                    Height="36"/>
                                <Border
                                    Visibility="{x:Bind ShowUnmuted, Mode=OneWay}"
                                    Background="{ThemeResource ChatUnreadBadgeBrush}"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Margin="0,0,-4,0"
                                    MinWidth="20"
                                    Height="20"
                                    CornerRadius="10">
                                    <TextBlock
                                        Text="{x:Bind UnreadCount, Mode=OneWay}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource ChatUnreadLabelBrush}"
                                        TextAlignment="Center"
                                        Margin="8,2,8,0"/>
                                </Border>
                                <Border
                                    Visibility="{x:Bind ShowMuted, Mode=OneWay}"
                                    Background="{ThemeResource ChatUnreadBadgeMutedBrush}"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Margin="0,0,-4,0"
                                    MinWidth="20"
                                    Height="20"
                                    CornerRadius="10">
                                    <TextBlock
                                        Text="{x:Bind UnreadMutedCount, Mode=OneWay}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource ChatUnreadLabelMutedBrush}"
                                        TextAlignment="Center"
                                        Margin="8,2,8,0"/>
                                </Border>
                            </Grid>
                            <TextBlock
                                Text="{x:Bind Title, Mode=OneWay}"
                                TextAlignment="Center"
                                TextWrapping="Wrap"
                                FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                Style="{StaticResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                    </DataTemplate>
                </controls:TopNavView.ItemTemplate>
                <controls:TopNavView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </controls:TopNavView.ItemsPanel>
                <controls:TopNavView.ItemContainerStyle>
                    <Style TargetType="controls:TopNavViewItem">
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="UseSystemFocusVisuals" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="controls:TopNavViewItem">
                                    <Grid Background="Transparent">
                                        <ContentPresenter
                                            x:Name="ContentPresenter"
                                            Grid.Column="1"
                                            Margin="8,4,8,4"
                                            TextWrapping="NoWrap"
                                            Foreground="{TemplateBinding Foreground}"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            Content="{TemplateBinding Content}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                            VerticalAlignment="Center"
                                            AutomationProperties.AccessibilityView="Raw"/>
                                        <Grid
                                            x:Name="SelectionIndicatorGrid"
                                            Margin="4,4,0,4"
                                            HorizontalAlignment="Left">
                                            <Rectangle
                                                x:Name="SelectionIndicator"
                                                Width="2"
                                                Fill="{ThemeResource NavigationViewSelectionIndicatorForeground}"
                                                Opacity="0"
                                                RadiusX="{Binding Source={ThemeResource ControlCornerRadius}, Converter={StaticResource TopLeftCornerRadiusDoubleValueConverter}}"
                                                RadiusY="{Binding Source={ThemeResource ControlCornerRadius}, Converter={StaticResource BottomRightCornerRadiusDoubleValueConverter}}" />
                                        </Grid>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </controls:TopNavView.ItemContainerStyle>
            </controls:TopNavView>
        </Border>
        
        <controls:MasterDetailView x:Name="MasterDetail" ViewStateChanged="OnStateChanged" Update="OnUpdate" Grid.Column="1" Grid.Row="1">
            <controls:MasterDetailView.Banner>
                <controls:PlaybackHeader x:Name="Playback"/>
            </controls:MasterDetailView.Banner>
            <controls:MasterDetailView.MasterHeader>
                <controls:ContentPageHeader x:Name="PageHeader" BackVisibility="Collapsed" Grid.Row="1">
                    <Grid x:Name="Header">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid x:Name="MainHeader" Padding="40,0,0,0" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto" MinWidth="12"/>
                            </Grid.ColumnDefinitions>
                            <Button
                                x:Name="ChatTabsSearch"
                                Click="Search_Click"
                                AutomationProperties.LandmarkType="Search"
                                Content="{CustomResource Search}"
                                Foreground="{ThemeResource TextControlPlaceholderForeground}"
                                Background="{ThemeResource PageBackgroundDarkBrush}"
                                BorderBrush="{ThemeResource PageBackgroundDarkBrush}"
                                HorizontalContentAlignment="Left"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                Height="32"
                                Padding="9,0,6,0"/>

                            <StackPanel x:Name="SettingsOptions" Orientation="Horizontal" Grid.Column="1">
                                <Button Foreground="{ThemeResource PageHeaderForegroundBrush}"
                                        Padding="{StaticResource EllipsisButtonPadding}"
                                        Style="{StaticResource EllipsisButton}"
                                        AutomationProperties.Name="{CustomResource AccDescrMoreOptions}"
                                        ToolTipService.ToolTip="{CustomResource AccDescrMoreOptions}">
                                    <FontIcon x:Name="EllipsisIcon"
                                              VerticalAlignment="Center"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              FontSize="16"
                                              Glyph="&#xE10C;"
                                              Height="{ThemeResource AppBarExpandButtonCircleDiameter}" />
                                    <Button.Flyout>
                                        <MenuFlyout x:Name="SettingsFlyout">
                                            <MenuFlyoutItem
                                                Click="EditName_Click"
                                                Text="{CustomResource EditName}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE104;"/>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem
                                                Click="LogOut_Click"
                                                Text="{CustomResource LogOut}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xF3B1;"/>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </StackPanel>
                            <StackPanel x:Name="ContactsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphToggleButton
                                    Glyph="&#xE91B;"
                                    CheckedGlyph="&#xE91E;"
                                    IsChecked="{x:Bind ViewModel.Contacts.IsSortedByEpoch, Mode=TwoWay}"
                                    Style="{StaticResource HeaderGlyphToggleButtonStyle}"
                                    FontFamily="{StaticResource TelegramThemeFontFamily}"
                                    AutomationProperties.Name="{CustomResource AccDescrContactSorting}"
                                    ToolTipService.ToolTip="{CustomResource AccDescrContactSorting}"
                                    IsOneWay="False"/>
                            </StackPanel>
                            <StackPanel x:Name="ChatsOptions" Orientation="Horizontal" Grid.Column="1">
                                <controls:GlyphButton
                                    x:Name="Proxy"
                                    Glyph="&#xE915;"
                                    Click="Proxy_Click"
                                    Visibility="Collapsed"
                                    FontFamily="{StaticResource TelegramThemeFontFamily}"
                                    Style="{StaticResource HeaderGlyphButtonStyle}"
                                    AutomationProperties.Name="{CustomResource ProxySettings}"
                                    ToolTipService.ToolTip="{CustomResource ProxySettings}"/>
                                <controls:GlyphToggleButton
                                    x:Name="Lock"
                                    Visibility="Collapsed"
                                    Glyph="&#xE1F7;"
                                    CheckedGlyph="&#xE1F6;"
                                    Click="Lock_Click"
                                    Style="{StaticResource HeaderGlyphToggleButtonStyle}"
                                    AutomationProperties.Name="{CustomResource AccDescrPasscodeLock}"
                                    ToolTipService.ToolTip="{CustomResource AccDescrPasscodeLock}"/>
                            </StackPanel>
                        </Grid>

                        <controls:GlyphButton
                            x:Name="BackButton"
                            Click="BackButton_Click"
                            Glyph="&#xE72B;"
                            Height="{ThemeResource AppBarThemeCompactHeight}"
                            FontSize="15"
                            Width="40"
                            Visibility="Collapsed"
                            AutomationProperties.Name="{CustomResource AccDescrGoBack}"
                            ToolTipService.ToolTip="{CustomResource AccDescrGoBack}"/>

                        <Grid x:Name="ManagePanel" Background="{ThemeResource PageHeaderBackgroundBrush}" Visibility="Collapsed" Grid.ColumnSpan="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <controls:GlyphButton
                                x:Name="ManageMute"
                                Command="{x:Bind ViewModel.Chats.ChatsNotifyCommand}"
                                Style="{StaticResource HeaderGlyphButtonStyle}"/>
                            <controls:GlyphButton
                                x:Name="ManageMark"
                                Command="{x:Bind ViewModel.Chats.ChatsMarkCommand}"
                                FontFamily="{StaticResource TelegramThemeFontFamily}"
                                Style="{StaticResource HeaderGlyphButtonStyle}"
                                Grid.Column="1"/>
                            <controls:GlyphButton
                                Glyph="&#xE7B8;"
                                Command="{x:Bind ViewModel.Chats.ChatsArchiveCommand}"
                                AutomationProperties.Name="{CustomResource Archive}"
                                ToolTipService.ToolTip="{CustomResource Archive}"
                                Style="{StaticResource HeaderGlyphButtonStyle}"
                                Grid.Column="2"/>
                            <controls:GlyphButton
                                Glyph="&#xE107;"
                                Command="{x:Bind ViewModel.Chats.ChatsDeleteCommand}"
                                AutomationProperties.Name="{CustomResource Delete}"
                                ToolTipService.ToolTip="{CustomResource Delete}"
                                Style="{StaticResource HeaderGlyphButtonStyle}"
                                Grid.Column="3"/>
                            <controls:GlyphButton
                                x:Name="ManageClear"
                                Glyph="&#xEA99;"
                                Command="{x:Bind ViewModel.Chats.ChatsClearCommand}"
                                AutomationProperties.Name="{CustomResource ClearHistory}"
                                ToolTipService.ToolTip="{CustomResource ClearHistory}"
                                Style="{StaticResource HeaderGlyphButtonStyle}"
                                Grid.Column="4"/>

                            <ContentControl
                                Content="{x:Bind ViewModel.Chats.SelectedItems.Count, Mode=OneWay}"
                                HorizontalContentAlignment="Center"
                                VerticalContentAlignment="Center"
                                FontWeight="SemiBold"
                                Padding="12,0"
                                Height="{ThemeResource AppBarThemeCompactHeight}"
                                Grid.Column="6">
                                <ContentControl.ContentTransitions>
                                    <TransitionCollection>
                                        <EntranceThemeTransition/>
                                    </TransitionCollection>
                                </ContentControl.ContentTransitions>
                                <ContentControl.Clip>
                                    <RectangleGeometry Rect="0,0,200,48"/>
                                </ContentControl.Clip>
                            </ContentControl>

                            <controls:GlyphButton
                                Click="Manage_Click"
                                Glyph="&#xE10A;"
                                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                Style="{StaticResource HeaderGlyphButtonStyle}"
                                Grid.Column="7"/>
                        </Grid>

                        <TextBox
                            x:Name="SearchField"
                            PlaceholderText="{CustomResource Search}"
                            Background="{ThemeResource PageBackgroundDarkBrush}"
                            BorderBrush="{ThemeResource PageBackgroundDarkBrush}"
                            PreventKeyboardDisplayOnProgrammaticFocus="True"
                            VerticalAlignment="Center"
                            TextChanged="Search_TextChanged"
                            KeyDown="Search_KeyDown"
                            Visibility="Collapsed"
                            Margin="0,0,12,0"
                            Grid.Column="1">
                            <TextBox.Header>
                                <ItemsControl
                                    x:Name="Test"
                                    x:Load="False"
                                    ItemsSource="{x:Bind ViewModel.Chats.SearchFilters}"
                                    Margin="-4,0,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="collections:ISearchChatsFilter">
                                            <StackPanel
                                                Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                                                Orientation="Horizontal"
                                                RequestedTheme="Dark"
                                                Margin="8,4,-4,4"
                                                CornerRadius="2">
                                                <TextBlock
                                                    Text="{x:Bind Glyph}"
                                                    Margin="8,0,8,0"
                                                    FontSize="16"
                                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                    VerticalAlignment="Center"/>
                                                <TextBlock
                                                    Text="{x:Bind Text}"
                                                    TextWrapping="NoWrap"
                                                    TextTrimming="CharacterEllipsis"
                                                    Margin="0,0,8,0"
                                                    FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                                    Style="{StaticResource BodyTextBlockStyle}"
                                                    VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </TextBox.Header>
                        </TextBox>
                    </Grid>
                </controls:ContentPageHeader>
            </controls:MasterDetailView.MasterHeader>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Normal"/>
                    <VisualState x:Name="TitleBar">
                        <VisualState.Setters>
                            <Setter Target="MasterDetail.BorderBrush" Value="{ThemeResource PageHeaderBackgroundBrush}"/>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <contract7Present:Grid.BackgroundTransition>
                    <contract7Present:BrushTransition/>
                </contract7Present:Grid.BackgroundTransition>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid x:Name="CallBanner" Visibility="Collapsed" Height="32" Background="{ThemeResource SystemControlHighlightAccentBrush}" Grid.Row="2">
                    <Button Command="{x:Bind ViewModel.ReturnToCallCommand}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Style="{StaticResource EmptyButtonStyle}">
                        <TextBlock Text="{CustomResource ReturnToCall}" Foreground="White" Style="{StaticResource CaptionTextBlockStyle}"/>
                    </Button>
                    <Border BorderThickness="0,0,0,1" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" IsHitTestVisible="False"/>
                </Grid>

                <Pivot x:Name="rpMasterTitlebar" SelectionChanged="Pivot_SelectionChanged" Style="{StaticResource HeaderlessPivotStyle}" Grid.Row="3">
                    <PivotItem x:Name="TabChats" Header="Chats">
                        <Grid>
                            <Grid x:Name="DialogsSearchPanel">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <ListView x:Load="False"
                                          x:Name="PippoFranco"
                                          SelectionMode="None"
                                          IsItemClickEnabled="True"
                                          ItemClick="SearchFilters_ItemClick"
                                          ContainerContentChanging="SearchFilters_ContainerContentChanging"
                                          ItemsSource="{x:Bind ViewModel.Chats.Search.Filters, Mode=OneWay}"
                                          Style="{StaticResource HorizontalListViewStyle}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock
                                                    Margin="12,0,8,0"
                                                    Text="&#xE77B;"
                                                    FontSize="16"
                                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                    Style="{StaticResource DisabledBodyTextBlockStyle}"
                                                    VerticalAlignment="Center"/>
                                                <TextBlock
                                                    TextWrapping="NoWrap"
                                                    TextTrimming="CharacterEllipsis"
                                                    Margin="0,0,12,0"
                                                    FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                                    Style="{StaticResource DisabledBodyTextBlockStyle}"
                                                    VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="MinWidth" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemContainerTransitions>
                                        <TransitionCollection/>
                                    </ListView.ItemContainerTransitions>
                                </ListView>

                                <ListView x:Name="DialogsSearchListView"
                                          ItemsSource="{x:Bind ChatsResults.View, Mode=OneWay}"
                                          IsItemClickEnabled="True"
                                          SelectionMode="Single"
                                          ItemClick="ListView_ItemClick"
                                          ChoosingItemContainer="DialogsSearchListView_ChoosingItemContainer"
                                          ContainerContentChanging="DialogsSearchListView_ContainerContentChanging"
                                          SelectionChanged="ListView_SelectionChanged"
                                          Grid.Row="1">
                                    <ListView.Header>
                                        <StackPanel Visibility="{x:Bind ViewModel.Chats.TopChats, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                                            <Border Visibility="{x:Bind ViewModel.Chats.TopChats.Count, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" Background="{ThemeResource PageBackgroundDarkBrush}">
                                                <TextBlock Text="{CustomResource ChatHints}" Padding="12,0,0,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                            </Border>
                                            <ListView SelectionMode="None"
                                                      IsItemClickEnabled="True"
                                                      ItemClick="ListView_ItemClick"
                                                      ContainerContentChanging="TopChats_ContainerContentChanging"
                                                      ItemsSource="{x:Bind ViewModel.Chats.TopChats, Mode=OneWay}"
                                                      Style="{StaticResource HorizontalListViewStyle}">
                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Width="72" Padding="0,12" Background="Transparent" ContextRequested="TopChat_ContextRequested">
                                                            <Grid Width="48" Height="48" Margin="12,0">
                                                                <controls:ProfilePicture IsHitTestVisible="False"/>
                                                                <Border Background="{ThemeResource ChatUnreadBadgeBrush}"
                                                                        MinWidth="20"
                                                                        Height="20"
                                                                        CornerRadius="10"
                                                                        VerticalAlignment="Top"
                                                                        HorizontalAlignment="Right"
                                                                        Margin="0,-2,-2,0"
                                                                        Visibility="Collapsed">
                                                                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                                               Foreground="{ThemeResource ChatUnreadLabelBrush}"
                                                                               TextAlignment="Center"
                                                                               Margin="4,2,4,0"/>
                                                                </Border>
                                                                <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                                                        Width="13"
                                                                        Height="13"
                                                                        CornerRadius="6.5"
                                                                        HorizontalAlignment="Right"
                                                                        VerticalAlignment="Bottom"
                                                                        Margin="0,0,1,1">
                                                                    <Border Background="{ThemeResource ChatOnlineBadgeBrush}"
                                                                            Width="9"
                                                                            Height="9"
                                                                            CornerRadius="4.5"/>
                                                                </Border>
                                                            </Grid>

                                                            <TextBlock Margin="0,8,0,0" TextAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" FontFamily="{ThemeResource EmojiThemeFontFamily}" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">
                                                        <Setter Property="MinWidth" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                                <ListView.ItemContainerTransitions>
                                                    <TransitionCollection/>
                                                </ListView.ItemContainerTransitions>
                                            </ListView>

                                            <Grid Visibility="{x:Bind ViewModel.Chats.Search.Local.Count, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" Background="{ThemeResource PageBackgroundDarkBrush}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{CustomResource Recent}" Padding="12,0,0,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                                <controls:GlyphButton Command="{x:Bind ViewModel.Chats.ClearRecentChatsCommand}" AllowFocusOnInteraction="False" Glyph="&#xE10A;" FontSize="12" Width="48" Height="32" Grid.Column="1"/>
                                            </Grid>
                                        </StackPanel>
                                    </ListView.Header>
                                    <ListView.ItemTemplateSelector>
                                        <selectors:SearchTemplateSelector>
                                            <selectors:SearchTemplateSelector.ChatTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,6">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition/>
                                                        </Grid.ColumnDefinitions>
                                                        <controls:ProfilePicture Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                        <Grid VerticalAlignment="Bottom" HorizontalAlignment="Left" Grid.Column="1">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Bottom"/>
                                                            <Grid Margin="4,0,0,-2" Grid.Column="1">
                                                                <Path Width="16" Height="16" Stretch="Uniform" Data="F1M33,0L23,3 12.999,0 9,9 0,13 2.999,23 0,33 9,37 12.999,46 23,43 33,46 37,37 46,33 43,23 46,13 37,9z M31.485,14.514L35.727,18.757 23.707,30.778 19.464,35.021 15.221,30.778 10.271,25.828 14.514,21.586 19.464,26.535z" Fill="{ThemeResource ChatVerifiedBadgeBrush}" StrokeThickness="0" />
                                                            </Grid>
                                                        </Grid>
                                                        <TextBlock VerticalAlignment="Top"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                                   FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   TextWrapping="NoWrap"
                                                                   Grid.Column="1"
                                                                   Grid.Row="1"
                                                                   MaxLines="1"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </selectors:SearchTemplateSelector.ChatTemplate>
                                            <selectors:SearchTemplateSelector.MessageTemplate>
                                                <DataTemplate>
                                                    <cells:ChatCell Margin="-12,0" Stroke="{ThemeResource ChatLastMessageStateBrush}" IsEnabled="False"/>
                                                </DataTemplate>
                                            </selectors:SearchTemplateSelector.MessageTemplate>
                                        </selectors:SearchTemplateSelector>
                                    </ListView.ItemTemplateSelector>
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemContainerTransitions>
                                        <TransitionCollection/>
                                    </ListView.ItemContainerTransitions>
                                    <ListView.GroupStyle>
                                        <GroupStyle>
                                            <GroupStyle.HeaderTemplate>
                                                <DataTemplate>
                                                    <!--<TextBlock Text="{Binding Key}" Style="{StaticResource BaseTextBlockStyle}"/>-->
                                                    <TextBlock Text="{Binding Key}" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                                </DataTemplate>
                                            </GroupStyle.HeaderTemplate>
                                            <GroupStyle.HeaderContainerStyle>
                                                <Style TargetType="ListViewHeaderItem">
                                                    <Setter Property="Background" Value="{ThemeResource PageBackgroundDarkBrush}" />
                                                    <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                    <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                                    <Setter Property="Margin" Value="0,0,0,0"/>
                                                    <Setter Property="Padding" Value="12,0,12,0"/>
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="VerticalContentAlignment" Value="Top" />
                                                    <Setter Property="MinHeight" Value="0"/>
                                                    <Setter Property="UseSystemFocusVisuals" Value="True" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ListViewHeaderItem">
                                                                <Border Visibility="{Binding Content.Count, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                            Visibility="{Binding Content.Key, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}">
                                                                        <ContentPresenter x:Name="ContentPresenter"
                                                                                          Margin="{TemplateBinding Padding}"
                                                                                          Content="{TemplateBinding Content}"
                                                                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                          ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                                    </Border>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </GroupStyle.HeaderContainerStyle>
                                        </GroupStyle>
                                    </ListView.GroupStyle>
                                </ListView>
                            </Grid>
                            <Grid x:Name="DialogsPanel" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                                <Grid x:Name="ChatsPanel">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Border x:Name="ChatTabs" x:Load="False">
                                        <Grid x:Name="ChatTabsView">
                                            <controls:TopNavView
                                                x:Name="ChatFilters"
                                                ItemsSource="{x:Bind ViewModel.Filters}"
                                                SelectedItem="{x:Bind ConvertFilter(ViewModel.SelectedFilter), Mode=TwoWay, BindBack=ConvertFilterBack}"
                                                IsItemClickEnabled="True"
                                                ItemClick="ChatFilter_ItemClick"
                                                ItemContextRequested="ChatFilter_ContextRequested"
                                                AllowDrop="True"
                                                CanDragItems="True"
                                                CanReorderItems="False"
                                                DragItemsStarting="ChatFilters_DragItemsStarting"
                                                DragItemsCompleted="ChatFilters_DragItemsCompleted"
                                                Height="40">
                                                <controls:TopNavView.ItemTemplate>
                                                    <DataTemplate x:DataType="viewModels:ChatFilterViewModel">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock
                                                                Text="{x:Bind Title, Mode=OneWay}"
                                                                FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                                                VerticalAlignment="Top"/>
                                                            <Border
                                                                Visibility="{x:Bind ShowUnmuted, Mode=OneWay}"
                                                                Background="{ThemeResource ChatUnreadBadgeBrush}"
                                                                MinWidth="20"
                                                                Height="20"
                                                                CornerRadius="10"
                                                                Margin="8,0,0,0">
                                                                <TextBlock
                                                                    Text="{x:Bind UnreadCount, Mode=OneWay}"
                                                                    Style="{StaticResource CaptionTextBlockStyle}"
                                                                    Foreground="{ThemeResource ChatUnreadLabelBrush}"
                                                                    TextAlignment="Center"
                                                                    Margin="6,2,6,0"/>
                                                            </Border>
                                                            <Border
                                                                Visibility="{x:Bind ShowMuted, Mode=OneWay}"
                                                                Background="{ThemeResource ChatUnreadBadgeMutedBrush}"
                                                                MinWidth="20"
                                                                Height="20"
                                                                CornerRadius="10"
                                                                Margin="8,0,0,0">
                                                                <TextBlock
                                                                    Text="{x:Bind UnreadMutedCount, Mode=OneWay}"
                                                                    Style="{StaticResource CaptionTextBlockStyle}"
                                                                    Foreground="{ThemeResource ChatUnreadLabelMutedBrush}"
                                                                    TextAlignment="Center"
                                                                    Margin="6,2,6,0"/>
                                                            </Border>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </controls:TopNavView.ItemTemplate>
                                            </controls:TopNavView>

                                            <Border BorderBrush="{ThemeResource PageHeaderBorderBrush}" BorderThickness="0,0,0,1" VerticalAlignment="Bottom"/>
                                        </Grid>
                                    </Border>

                                    <!--IsItemClickEnabled="True"
                                                            SelectionMode="{x:Bind ViewModel.Chats.SelectionMode, Mode=TwoWay}"-->
                                    <local:ChatsPage x:Name="ChatsList"
                                                     AutomationProperties.LandmarkType="Main"
                                                     Main="{x:Bind ViewModel}"
                                                     DataContext="{x:Bind ViewModel.Chats}"
                                                     ItemsSource="{x:Bind ViewModel.Chats.Items, Mode=OneWay}"
                                                     SelectionMode2="{x:Bind ViewModel.Chats.SelectionMode, Mode=TwoWay}"
                                                     AllowSelection="True"
                                                     Grid.Row="1">
                                        <ListView.Header>
                                            <Grid x:Name="ArchivedChatsPresenter">
                                                <Border x:Name="ArchivedChatsPanel">
                                                    <cells:ChatCell
                                                        x:Name="ArchivedChats"
                                                        Click="ArchivedChats_Click"
                                                        ContextRequested="ArchivedChats_ContextRequested"
                                                        Visibility="Collapsed"
                                                        Stroke="{ThemeResource ChatLastMessageStateBrush}"
                                                        SelectionStroke="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                                        Accent="{ThemeResource SystemAccentColor}" />
                                                </Border>
                                                <Border VerticalAlignment="Bottom">
                                                    <Border x:Name="ArchivedChatsCompactPanel" Background="{ThemeResource PageBackgroundDarkBrush}">
                                                        <cells:ChatCell
                                                            Content="{CustomResource ArchivedChats}"
                                                            Click="ArchivedChats_Click"
                                                            ContextRequested="ArchivedChats_ContextRequested"
                                                            Visibility="{x:Bind ArchivedChats.Visibility, Mode=OneWay}"
                                                            Padding="12,6"/>
                                                    </Border>
                                                </Border>
                                            </Grid>
                                        </ListView.Header>
                                    </local:ChatsPage>
                                </Grid>

                                <Grid x:Name="FolderPanel" Visibility="Collapsed" Grid.RowSpan="2">
                                    <Border x:Name="FolderShadow" Background="{ThemeResource MessageOverlayBackgroundBrush}"/>
                                    <local:ChatsPage
                                        x:Name="FolderList"
                                        AutomationProperties.LandmarkType="Main"
                                        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                        Main="{x:Bind ViewModel}"
                                        DataContext="{x:Bind ViewModel.Folder, Mode=OneWay}"
                                        ItemsSource="{x:Bind ViewModel.Folder.Items, Mode=OneWay}"
                                        SelectionMode2="{x:Bind ViewModel.Folder.SelectionMode, Mode=TwoWay}"/>
                                </Grid>

                                <Grid
                                    VerticalAlignment="Bottom"
                                    HorizontalAlignment="Center"
                                    Margin="12"
                                    Grid.Row="1">
                                    <Rectangle
                                        x:Name="UpdateShadow"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Fill="{ThemeResource StartButtonBackground}"
                                        Margin="2"
                                        RadiusX="18"
                                        RadiusY="18"/>
                                    <controls:BadgeButton
                                        x:Name="UpdateCloud"
                                        Command="{x:Bind ViewModel.UpdateAppCommand}"
                                        Visibility="{x:Bind ViewModel.IsUpdateAvailable, Mode=OneWay}"
                                        Content="{CustomResource UpdateTelegram}"
                                        Style="{StaticResource StartButtonStyle}"
                                        Padding="16,3,16,5"
                                        Radius="18"
                                        Margin="0"
                                        Height="36"/>
                                </Grid>

                                <controls:UndoView x:Name="Undo" VerticalAlignment="Bottom" Grid.Row="1"/>
                            </Grid>
                        </Grid>
                    </PivotItem>
                    <PivotItem Header="{CustomResource Contacts}">
                        <Grid>
                            <ListView x:Name="ContactsSearchListView"
                                      ItemsSource="{x:Bind ContactsResults.View, Mode=OneWay}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None"
                                      ItemClick="ListView_ItemClick"
                                      SelectionChanged="ListView_SelectionChanged"
                                      ContainerContentChanging="ContactsSearchListView_ContainerContentChanging">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,4,0,8">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <controls:ProfilePicture Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                            <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom" Grid.Row="0"/>
                                            <TextBlock VerticalAlignment="Top"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextWrapping="NoWrap"
                                                       Grid.Column="1"
                                                       Grid.Row="1"
                                                       MaxLines="1"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemContainerTransitions>
                                    <TransitionCollection/>
                                </ListView.ItemContainerTransitions>
                                <ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Key}" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="ListViewHeaderItem">
                                                <Setter Property="Background" Value="{ThemeResource PageBackgroundDarkBrush}" />
                                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                                <Setter Property="Margin" Value="0,0,0,0"/>
                                                <Setter Property="Padding" Value="12,0,12,0"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="UseSystemFocusVisuals" Value="True" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListViewHeaderItem">
                                                            <Border Visibility="{Binding Content.Count, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        Visibility="{Binding Content.Key, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}">
                                                                    <ContentPresenter x:Name="ContentPresenter"
                                                                                      Margin="{TemplateBinding Padding}"
                                                                                      Content="{TemplateBinding Content}"
                                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                      ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                                </Border>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>
                            </ListView>
                            <Grid x:Name="ContactsPanel">
                                <ListView x:Name="UsersListView"
                                          ItemsSource="{x:Bind ViewModel.Contacts.Items, Mode=OneWay}"
                                          IsItemClickEnabled="True"
                                          ItemClick="ListView_ItemClick"
                                          ChoosingItemContainer="UsersListView_ChoosingItemContainer"
                                          ContainerContentChanging="UsersListView_ContainerContentChanging"
                                          SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,6">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition/>
                                                </Grid.ColumnDefinitions>
                                                <controls:ProfilePicture IsEnabled="False" Width="36" Height="36" Margin="0,0,12,0" Grid.RowSpan="2"/>

                                                <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Grid.Column="1" VerticalAlignment="Bottom"/>
                                                <TextBlock VerticalAlignment="Top"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="NoWrap"
                                                           Grid.Column="1"
                                                           Grid.Row="1"
                                                           MaxLines="1"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.Header>
                                        <StackPanel>
                                            <controls:BadgeButton
                                                Content="{CustomResource AddPeopleNearby}"
                                                Click="ChatsNearby_Click"
                                                Style="{StaticResource GlyphBadgeButtonStyle}"
                                                Glyph="&#xE1C4;"/>
                                            
                                            <controls:BadgeButton
                                                Content="{CustomResource CreateNewContact}"
                                                Click="NewContact_Click"
                                                Style="{StaticResource GlyphBadgeButtonStyle}"
                                                Glyph="&#xE8FA;"/>

                                            <Border Background="{ThemeResource PageBackgroundDarkBrush}">
                                                <TextBlock Text="{x:Bind ConvertSortedBy(ViewModel.Contacts.IsSortedByEpoch), Mode=OneWay}" Padding="12,0,0,0" Style="{StaticResource SettingsGroupTextBlockStyle}"/>
                                            </Border>
                                        </StackPanel>
                                    </ListView.Header>
                                    <!--<ListView.ItemContainerTransitions>
                                         <TransitionCollection/>
                                         </ListView.ItemContainerTransitions>-->
                                </ListView>
                            </Grid>
                        </Grid>
                    </PivotItem>
                    <PivotItem x:Name="Calls" Header="{CustomResource Calls}">
                        <ListView ItemsSource="{x:Bind ViewModel.Calls.Items}"
                                  ItemClick="ListView_ItemClick"
                                  IsItemClickEnabled="True"
                                  ContainerContentChanging="Calls_ContainerContentChanging"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <cells:CallCell ContextRequested="Call_ContextRequested"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <!--<ListView.ItemContainerTransitions>
                                 <TransitionCollection/>
                                 </ListView.ItemContainerTransitions>-->
                        </ListView>
                    </PivotItem>
                    <PivotItem Header="{CustomResource Settings}">
                        <Grid>
                            <ListView ItemsSource="{x:Bind ViewModel.Settings.Results}"
                                      SelectionMode="None"
                                      ContainerContentChanging="Settings_ContainerContentChanging"
                                      Grid.Row="1">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Padding" Value="0"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <controls:BadgeButton Style="{StaticResource GlyphSubbadgeBadgeButtonStyle}"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <local:SettingsPage x:Name="SettingsView" MasterDetail="{x:Bind MasterDetail}"/>
                        </Grid>
                    </PivotItem>
                </Pivot>

                <Border x:Name="Separator" BorderBrush="{ThemeResource PageHeaderBorderBrush}" BorderThickness="1,0,0,0" IsHitTestVisible="False" HorizontalAlignment="Right" Margin="0,-48,0,0" Grid.RowSpan="4" Grid.Row="1"/>
            </Grid>
        </controls:MasterDetailView>

        <controls:ConfettiView
            x:Name="Confetti"
            x:Load="False"
            Completed="Confetti_Completed"
            IsHitTestVisible="False"
            Grid.ColumnSpan="2"
            Grid.Row="1"/>
    </Grid>
</Page>
