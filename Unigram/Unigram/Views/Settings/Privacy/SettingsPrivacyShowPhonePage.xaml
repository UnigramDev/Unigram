<local:HostedPage
    x:Class="Unigram.Views.Settings.Privacy.SettingsPrivacyShowPhonePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views"
    xmlns:controls="using:Unigram.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <DrillInNavigationTransitionInfo/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <local:HostedPage.Header>
        <controls:ButtonPageHeader
            Text="{CustomResource PrivacyPhone}"
            Command="{x:Bind ViewModel.SendCommand}"
            Glyph="&#xE10B;"/>
    </local:HostedPage.Header>

    <Grid Background="{ThemeResource PageBackgroundDarkBrush}">
        <ScrollViewer x:Name="ScrollingHost"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto">
            <StackPanel>
                <controls:HeaderedControl
                    Header="{CustomResource PrivacyPhoneTitle}"
                    Footer="{CustomResource PrivacyPhoneInfo}">
                    <controls:PrivacyRadioButton
                        Content="{CustomResource LastSeenEverybody}"
                        Value="{x:Bind ViewModel.ShowPhone.SelectedItem, Mode=TwoWay}"
                        Type="AllowAll"
                        GroupName="Show"
                        Margin="12,6,0,0"/>
                    <Rectangle Fill="{ThemeResource TelegramSeparatorMediumBrush}" Height="1" Margin="12,6,0,6"/>

                    <controls:PrivacyRadioButton
                        Content="{CustomResource LastSeenContacts}"
                        Value="{x:Bind ViewModel.ShowPhone.SelectedItem, Mode=TwoWay}"
                        Type="AllowContacts"
                        GroupName="Show"
                        Margin="12,0,0,0"/>
                    <Rectangle Fill="{ThemeResource TelegramSeparatorMediumBrush}" Height="1" Margin="12,6,0,6"/>

                    <controls:PrivacyRadioButton
                        Content="{CustomResource LastSeenNobody}"
                        Value="{x:Bind ViewModel.ShowPhone.SelectedItem, Mode=TwoWay}"
                        Type="DisallowAll"
                        GroupName="Show"
                        Margin="12,0,0,0"/>
                    <Rectangle Fill="{ThemeResource TelegramSeparatorMediumBrush}" Height="1" Margin="12,6,0,0"/>
                </controls:HeaderedControl>

                <controls:HeaderedControl
                    Header="{CustomResource PrivacyPhoneTitle2}"
                    Footer="{x:Bind ConvertFooter(ViewModel.AllowFindingByPhoneNumber.SelectedItem), Mode=OneWay}"
                    Visibility="{x:Bind ConvertFinding(ViewModel.ShowPhone.SelectedItem), Mode=OneWay}">
                    <controls:PrivacyRadioButton
                        Content="{CustomResource LastSeenEverybody}"
                        Value="{x:Bind ViewModel.AllowFindingByPhoneNumber.SelectedItem, Mode=TwoWay}"
                        Type="AllowAll"
                        GroupName="Allow"
                        Margin="12,6,0,0"/>
                    <Rectangle Fill="{ThemeResource TelegramSeparatorMediumBrush}" Height="1" Margin="12,6,0,6"/>

                    <controls:PrivacyRadioButton
                        Content="{CustomResource LastSeenContacts}"
                        Value="{x:Bind ViewModel.AllowFindingByPhoneNumber.SelectedItem, Mode=TwoWay}"
                        Type="AllowContacts"
                        GroupName="Allow"
                        Margin="12,0,0,0"/>
                    <Rectangle Fill="{ThemeResource TelegramSeparatorMediumBrush}" Height="1" Margin="12,6,0,0"/>
                </controls:HeaderedControl>
                
                <controls:HeaderedControl
                    Header="{CustomResource AddExceptions}"
                    Footer="{CustomResource PrivacyPhoneInfo2}">
                    <controls:BadgeButton
                        Content="{CustomResource AlwaysAllow}"
                        Command="{x:Bind ViewModel.ShowPhone.AlwaysCommand}"
                        Visibility="{x:Bind ConvertAlways(ViewModel.ShowPhone.SelectedItem), Mode=OneWay}"
                        Badge="{x:Bind ViewModel.ShowPhone.AllowedBadge, Mode=OneWay}"/>
                    
                    <controls:BadgeButton
                        Content="{CustomResource NeverAllow}"
                        Command="{x:Bind ViewModel.ShowPhone.NeverCommand}"
                        Visibility="{x:Bind ConvertNever(ViewModel.ShowPhone.SelectedItem), Mode=OneWay}"
                        Badge="{x:Bind ViewModel.ShowPhone.RestrictedBadge, Mode=OneWay}"/>
                </controls:HeaderedControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</local:HostedPage>
