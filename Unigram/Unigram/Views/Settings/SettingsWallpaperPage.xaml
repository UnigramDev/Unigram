<Page
    x:Class="Unigram.Views.Settings.SettingsWallPaperPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views.Settings"
    xmlns:controls="using:Unigram.Controls"
    xmlns:selectors="using:Unigram.Selectors"
    xmlns:tl="using:Telegram.Api.TL"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <controls:PageHeader Text="Chat Background"/>

        <GridView ItemsSource="{x:Bind ViewModel.Items}"
                  SelectionMode="Multiple"
                  Grid.Row="1">
            <GridView.ItemTemplateSelector>
                <selectors:WallPaperTemplateSelector>
                    <selectors:WallPaperTemplateSelector.ItemTemplate>
                        <DataTemplate x:DataType="tl:TLWallPaper">
                            <controls:ImageView Source="{x:Bind Full, Converter={StaticResource DefaultPhotoConverter}}"
                                                Constraint="{x:Bind Full}"
                                                MaxWidth="200"
                                                IsHitTestVisible="False"/>
                        </DataTemplate>
                    </selectors:WallPaperTemplateSelector.ItemTemplate>
                </selectors:WallPaperTemplateSelector>
            </GridView.ItemTemplateSelector>
        </GridView>
    </Grid>
</Page>
