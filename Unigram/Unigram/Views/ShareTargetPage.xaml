<Page
    x:Class="Unigram.Views.ShareTargetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Unigram.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <RelativePanel x:Name="rpMain"
                       Visibility="Visible">
            <Grid x:Name="Header"
                  Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                  Height="{ThemeResource AppBarThemeCompactHeight}"
                  RelativePanel.AlignLeftWithPanel="True"
                  RelativePanel.AlignRightWithPanel="True"
                  RelativePanel.AlignTopWithPanel="True">
                <TextBlock x:Name="tblPivotTitle"
                           Text="SHARE"
                           Style="{ThemeResource BaseTextBlockStyle}"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           Margin="12,0" />
            </Grid>
            <ProgressBar x:Name="prgSendStatus"
                         HorizontalAlignment="Stretch"
                         Height="4" 
                         Margin="0" 
                         IsEnabled="True"
                         Visibility="Visible"
                         RelativePanel.AlignLeftWithPanel="True"
                         RelativePanel.AlignRightWithPanel="True"
                         RelativePanel.Below="Header"/>
            <ScrollViewer RelativePanel.AlignLeftWithPanel="True"
                          RelativePanel.AlignRightWithPanel="True"
                          RelativePanel.Below="prgSendStatus"
                          RelativePanel.AlignBottomWithPanel="True">

                <ListView x:Name="lvMasterChats"
                          Margin="0"
                          ItemsSource="{x:Bind ViewModel.Dialogs}"
                          IsItemClickEnabled="True"
                          SelectionMode="None"
                          ItemClick="lvMasterChats_ItemClick">
                    <!--<FlyoutBase.AttachedFlyout>
                        <Flyout>
                            <RelativePanel>
                                <TextBlock x:Name="tblTitleFlyout"
                                           Text="Share with this chat?"
                                           TextWrapping="Wrap"
                                           Margin="0,12,0,60"
                                           Style="{ThemeResource TitleTextBlockStyle}"
                                           RelativePanel.AlignHorizontalCenterWithPanel="True"
                                           RelativePanel.AlignTopWithPanel="True"/>
                                <StackPanel Orientation="Horizontal"
                                            RelativePanel.AlignHorizontalCenterWithPanel="True"
                                            RelativePanel.Below="tblTitleFlyout"
                                            RelativePanel.AlignBottomWithPanel="True">
                                    <Button x:Name="btnNo"
                                            Margin="12,0"
                                            Width="140"
                                            Content="No"
                                            Click="btnNo_Click"/>
                                    <Button x:Name="btnYes"
                                            Margin="12,0"
                                            Width="140"
                                            Content="Yes"
                                            Click="btnYes_Click"/>
                                </StackPanel>
                            </RelativePanel>
                        </Flyout>
                    </FlyoutBase.AttachedFlyout>-->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <controls:SimpleDialogListViewItem/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.Header>
                        <controls:SearchTextBox PlaceholderText="Search..." Margin="12,8" InputScope="Search"/>
                    </ListView.Header>
                </ListView>
            </ScrollViewer>
        </RelativePanel>
    </Grid>
</Page>
