<controls:ContentPopup
    x:Class="Unigram.Views.Popups.SelectRadioPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views.Popups"
    xmlns:controls="using:Unigram.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TITLE"
    PrimaryButtonText="Button1"
    SecondaryButtonText="Button2"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <ContentDialog.Resources>
        <Style x:Key="RectangleStyle" TargetType="Rectangle">
            <Setter Property="Fill" Value="{ThemeResource TelegramSeparatorMediumBrush}"/>
            <Setter Property="Height" Value="1"/>
        </Style>
    </ContentDialog.Resources>
    
    <Grid Background="{ThemeResource PageBackgroundDarkBrush}">
        <controls:HeaderedControl x:Name="LayoutRoot" Margin="0,0,0,16"/>
    </Grid>
</controls:ContentPopup>
