<Page
    x:Class="Unigram.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views"
    xmlns:controls="using:Unigram.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Transitions>
        <TransitionCollection>
            <EdgeUIThemeTransition Edge="Right"/>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!-- Titlebar -->
            <controls:ContentPageHeader Grid.Row="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="About" Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Margin="12,0"/>
                </Grid>
            </controls:ContentPageHeader>
            
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          VerticalScrollMode="Auto"
                          Grid.Row="1">
                <!-- Main content -->
                <StackPanel Padding="12"
                            ScrollViewer.VerticalScrollMode="Auto">
                    <!-- App logo and info-->
                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,12">
                        <!-- Logo -->
                        <Grid Height="80"
                          Width="64">
                            <!-- Background -->
                            <Ellipse Height="64"
                                     Width="64"
                                     Fill="White"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"/>
                            <Ellipse Height="60"
                                     Width="60"
                                     Fill="{ThemeResource TelegramBackgroundAccentBrush}"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"/>
                            <!-- Logo -->
                            <Ellipse Height="60"
                                     Width="60"
                                     Margin="0,0,0,-6"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="ms-appx:///Assets/Logos/Square150x150Logo/Square150x150Logo.png" />
                                </Ellipse.Fill>
                            </Ellipse>
                        </Grid>
                        <!-- Info -->
                        <StackPanel Orientation="Vertical"
                                    Margin="16,0,0,0"
                                    VerticalAlignment="Center">

                            <TextBlock x:Uid="About-App-Title"
                                       Text="Unigram"
                                       Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Uid="About-App-Version"
                                           Text="Version"
                                           Foreground="{StaticResource SystemControlForegroundBaseMediumBrush}"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock x:Name="tblAppVersion"
                                           Padding="4,0,16,0"
                                           Text="Prototype"
                                           Foreground="{StaticResource SystemControlForegroundBaseMediumBrush}"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock x:Uid="About-Thanks-Intro"
                               Text="Thank you for using Unigram! We woudn't have been able to get this far without all help of the community."
                               TextWrapping="Wrap"
                               FontSize="14"
                               Margin="0,0,0,16"/>

                    <!-- Useful links -->
                    <Button Content="Privacy Policy"
                            Padding="0,6"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnPrivacy_Click"/>
                    <Button Content="Frequently Asked Questions"
                            Padding="0,6"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnFAQ_Click"/>
                    <Button Content="Unigram Website"
                            Padding="0,6,0,12"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnWebsite_Click"/>

                    <!-- Support -->
                    <TextBlock Margin="0,12,0,6"
                               Text="Support"
                               FontSize="20"
                               Style="{StaticResource SubtitleTextBlockStyle}" />
                    <Grid Margin="0,0,0,12">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Content="Email"
                                Grid.Row="0"
                                Grid.Column="0"
                                Padding="0,6,8,6"
                                Style="{ThemeResource TextBlockButtonStyle}"
                                Click="btnSupportEmail_Click"/>
                        <Button Content="Github"
                                Grid.Row="0"
                                Grid.Column="1"
                                Padding="16,6,8,6"
                                Style="{ThemeResource TextBlockButtonStyle}"
                                Click="btnSupportGitHub_Click"/>
                    </Grid>


                    <!-- Social -->
                    <TextBlock Margin="0,12,0,6"
                               Text="Social Media"
                               FontSize="20"
                               Style="{StaticResource SubtitleTextBlockStyle}" />
                    <Button Content="Follow @UnigramApp on Twitter"
                            Padding="0,6"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnSocialTwitter_Click"/>
                    <Button Content="Like us on Facebook"
                            Padding="0,6,0,12"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnSocialFacebook_Click"/>

                    <!-- Special thanks -->
                    <TextBlock Margin="0,12,0,6"
                               Text="Special thanks"
                               FontSize="20"
                               Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="We want to especially thank these people for their help and knowledge while building Unigram:"
                               FontSize="14"
                               TextWrapping="Wrap"
                               Margin="0,0,0,6"/>
                    <Button Content="Telegram Desktop"
                            Padding="0,6"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnThanksTelegramDesktop_Click"/>
                    <Button Content="Gregory Klyushnikov"
                            Padding="0,6,0,12"
                            Style="{ThemeResource TextBlockButtonStyle}"
                            Click="btnThanksGregory_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
