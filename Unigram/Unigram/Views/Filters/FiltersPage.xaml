<Page
    x:Class="Unigram.Views.Filters.FiltersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unigram.Views.Filters"
    xmlns:controls="using:Unigram.Controls"
    xmlns:td="using:Telegram.Td.Api"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource PageBackgroundDarkBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <controls:PageHeader Text="Filters"/>

        <controls:TableListView
            ItemsSource="{x:Bind ViewModel.Items}"
            VerticalContentAlignment="Top"
            Grid.Row="1">
            <ListView.Header>
                <controls:HeaderedControl
                    Footer="[Display filter tabs on main screen for quick switching.]">
                    <ToggleSwitch
                        Header="[Show Tabs]"
                        Style="{StaticResource CheckButtonStyle}"/>
                </controls:HeaderedControl>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="td:ChatFilter">
                    <TextBlock Text="{x:Bind Title}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Footer>
                <controls:HeaderedControl>
                    <controls:BadgeButton
                        Command="{x:Bind ViewModel.AddCommand}"
                        Content="[Create new filter]"/>
                </controls:HeaderedControl>
            </ListView.Footer>
        </controls:TableListView>
    </Grid>
</Page>
