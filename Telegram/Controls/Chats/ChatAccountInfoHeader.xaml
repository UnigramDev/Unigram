<Grid x:Class="Telegram.Controls.Chats.ChatAccountInfoHeader"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Telegram.Controls"
      mc:Ignorable="d"
      d:DesignHeight="300"
      d:DesignWidth="400"
      Background="Transparent"
      MinHeight="48"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Top"
      BorderThickness="0"
      Padding="0"
      Margin="0">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border BorderBrush="{ThemeResource NavigationViewContentGridBorderBrush}"
                BorderThickness="0,0,0,1"
                Grid.ColumnSpan="2"
                Grid.RowSpan="2" />

        <Border x:Name="PremiumUserRoot"
                Height="32">
            <RichTextBlock x:Name="PremiumUser"
                           FontFamily="{ThemeResource EmojiThemeFontFamilyWithSymbols}"
                           Style="{StaticResource CaptionRichTextBlockStyle}"
                           IsTextSelectionEnabled="False"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                <Paragraph>
                    <Span x:Name="PremiumUserText" />
                </Paragraph>
            </RichTextBlock>
        </Border>

        <RichTextBlock x:Name="PayingUser"
                       FontFamily="{ThemeResource EmojiThemeFontFamilyWithSymbols}"
                       Style="{StaticResource CaptionRichTextBlockStyle}"
                       IsTextSelectionEnabled="False"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="0,8"
                       Grid.Row="1">
            <Paragraph>
                <Span x:Name="PayingUserText" />
            </Paragraph>
        </RichTextBlock>
    </Grid>
</Grid>
