<controls:AspectView x:Class="Telegram.Controls.Gallery.GalleryContent"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:controls="using:Telegram.Controls"
                     mc:Ignorable="d"
                     d:DesignHeight="300"
                     d:DesignWidth="400">
    <controls:AspectView.Background>
        <ImageBrush x:Name="ThumbnailTexture"
                    Stretch="UniformToFill"
                    AlignmentX="Center"
                    AlignmentY="Center" />
    </controls:AspectView.Background>

    <Image x:Name="Texture"
           Stretch="UniformToFill" />

    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <TextBlock x:Name="DocumentName"
                   Margin="0,0,0,80" />
    </StackPanel>

    <controls:FileButton x:Name="Button"
                         Click="Button_Click"
                         ManipulationMode="System"
                         Style="{StaticResource ImmersiveFileButtonStyle}" />

    <Border x:Name="Panel"
            IsHitTestVisible="False" />

    <controls:ImageTextSelection x:Name="Selection"
                                 Visibility="Collapsed" />

</controls:AspectView>
