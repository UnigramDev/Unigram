<UserControl x:Class="Telegram.Controls.Stories.StoryLiveInteractionBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Telegram.Controls"
             xmlns:chats="using:Telegram.Controls.Chats"
             xmlns:drawers="using:Telegram.Controls.Drawers"
             xmlns:messages="using:Telegram.Controls.Messages"
             xmlns:local="using:Telegram.Controls.Stories"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <UserControl.Resources>
        <Style x:Key="AccentButtonStyle"
               TargetType="local:StoryPaidButton"
               BasedOn="{StaticResource DefaultButtonStyle}">
            <Setter Property="Foreground"
                    Value="#FFFFFF" />
            <Setter Property="Background"
                    Value="{ThemeResource AccentButtonBackground}" />
            <Setter Property="BackgroundSizing"
                    Value="OuterBorderEdge" />
            <Setter Property="BorderBrush"
                    Value="{ThemeResource AccentButtonBorderBrush}" />
            <Setter Property="CornerRadius"
                    Value="{ThemeResource ControlCornerRadius}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:StoryPaidButton">
                        <ContentPresenter x:Name="ContentPresenter"
                                          Background="{TemplateBinding Background}"
                                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentTransitions="{TemplateBinding ContentTransitions}"
                                          CornerRadius="{TemplateBinding CornerRadius}"
                                          Padding="{TemplateBinding Padding}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                          AutomationProperties.AccessibilityView="Raw">

                            <ContentPresenter.BackgroundTransition>
                                <BrushTransition Duration="0:0:0.083" />
                            </ContentPresenter.BackgroundTransition>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="ReadOnlyStates">
                                    <VisualState x:Name="NotReadOnly" />
                                    <VisualState x:Name="ReadOnly">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                                           Storyboard.TargetName="ContentPresenter">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource SystemControlForegroundBaseLowBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush"
                                                                           Storyboard.TargetName="ContentPresenter">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource SystemControlForegroundBaseLowBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <!--<VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonBackgroundPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonBorderBrushPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonForegroundPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>

                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonBackgroundPressed}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonBorderBrushPressed}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonForegroundPressed}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>

                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonBackgroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonBorderBrushDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                           Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{ThemeResource AccentButtonForegroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>-->
                            </VisualStateManager.VisualStateGroups>
                        </ContentPresenter>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid MinHeight="48">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid Margin="0,0,4,0">
            <Border Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource SettingsItemBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="24" />
            <TextBlock Text="&#xEAEE;"
                       FontFamily="{StaticResource SymbolThemeFontFamily}"
                       FontSize="24"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
            <controls:AnimatedGlyphToggleButton x:Name="ShowHideButton"
                                                Click="ShowHide_Click"
                                                Glyph="&#xEAEF;"
                                                CheckedGlyph="&#xEAF0;"
                                                FontSize="24"
                                                CornerRadius="20"
                                                IsOneWay="False" />
        </Grid>

        <Border x:Name="BackgroundGrid"
                Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                BorderBrush="{ThemeResource SettingsItemBorderBrush}"
                BorderThickness="1"
                CornerRadius="24"
                Grid.Column="1" />

        <Grid x:Name="MessagePanel"
              Canvas.ZIndex="1"
              Grid.Column="1">
            <FlyoutBase.AttachedFlyout>
                <Flyout x:Name="EmojiFlyout"
                        Placement="Top"
                        ShowMode="Transient">
                    <Flyout.FlyoutPresenterStyle>
                        <Style TargetType="FlyoutPresenter"
                               BasedOn="{StaticResource CommandFlyoutPresenterStyle}">
                            <Setter Property="Padding"
                                    Value="0" />
                        </Style>
                    </Flyout.FlyoutPresenterStyle>
                    <drawers:EmojiDrawer x:Name="EmojiPanel"
                                         ItemClick="Emoji_ItemClick"
                                         Width="300"
                                         Height="360" />
                </Flyout>
            </FlyoutBase.AttachedFlyout>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="ButtonAlias"
                             Click="ButtonAlias_Click"
                             Style="{StaticResource EmptyHyperlinkButtonStyle}"
                             AutomationProperties.Name="{CustomResource SendMessageAsTitle}"
                             ToolTipService.ToolTip="{CustomResource SendMessageAsTitle}"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Bottom"
                             Visibility="Collapsed"
                             Margin="8,8,-8,8"
                             Grid.Row="1">
                <controls:ProfilePicture x:Name="PhotoAlias"
                                         Size="32" />
            </HyperlinkButton>

            <controls:FormattedTextBox x:Name="MessageField"
                                       TextChanged="MessageField_TextChanged"
                                       Accept="MessageField_Accept"
                                       GotFocus="MessageField_GotFocus"
                                       LostFocus="MessageField_LostFocus"
                                       PlaceholderText="{CustomResource VoipMessage}"
                                       Style="{StaticResource DefaultChatTextBoxStyle}"
                                       Background="Transparent"
                                       BorderThickness="0"
                                       Height="0"
                                       MinHeight="0"
                                       Padding="16,0,0,0"
                                       Grid.Column="1" />
            <messages:CustomEmojiCanvas x:Name="CustomEmoji"
                                        IsHitTestVisible="False"
                                        Margin="14,11,0,0"
                                        Grid.Column="1" />

            <controls:GlyphButton x:Name="Emoji"
                                  Click="Emoji_Click"
                                  Glyph="&#xE76E;"
                                  AutomationProperties.Name="{CustomResource Emoji}"
                                  AllowFocusOnInteraction="False"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Bottom"
                                  Width="48"
                                  Height="48"
                                  FontSize="24"
                                  CornerRadius="20"
                                  Grid.Column="2" />

            <Grid x:Name="SendRoot"
                  Grid.Column="3">
                <controls:GlyphButton x:Name="Paid"
                                      Click="Paid_Click"
                                      SizeChanged="Button_SizeChanged"
                                      Glyph="&#xEACE;"
                                      AutomationProperties.Name="{CustomResource Emoji}"
                                      AllowFocusOnInteraction="False"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Bottom"
                                      Width="48"
                                      Height="48"
                                      FontSize="24"
                                      CornerRadius="20"
                                      Margin="-8,0,0,0"
                                      Grid.Column="2" />

                <chats:ChatSendButton x:Name="SendMessage"
                                      Click="Send_Click"
                                      ContextRequested="Send_ContextRequested"
                                      SizeChanged="Button_SizeChanged"
                                      AutomationProperties.Name="{CustomResource Send}"
                                      ToolTipService.ToolTip="{CustomResource Send}"
                                      Foreground="{ThemeResource TelegramBackgroundAccentBrush}"
                                      AllowFocusOnInteraction="False"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Bottom"
                                      Visibility="Collapsed"
                                      Width="48"
                                      Height="48"
                                      Margin="-8,0,0,0"
                                      CornerRadius="20"
                                      Grid.Column="2" />

                <local:StoryPaidButton x:Name="btnPaidMessage"
                                       ContextRequested="Paid_ContextRequested"
                                       SizeChanged="Button_SizeChanged"
                                       Click="Send_Click"
                                       Style="{StaticResource AccentButtonStyle}"
                                       FontFamily="{StaticResource EmojiThemeFontFamilyWithSymbols}"
                                       BorderBrush="{ThemeResource AccentButtonBackground}"
                                       AutomationProperties.Name="{CustomResource AccDescrBotCommands}"
                                       ToolTipService.ToolTip="{CustomResource AccDescrBotCommands}"
                                       AllowFocusOnInteraction="False"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Bottom"
                                       Visibility="Collapsed"
                                       CornerRadius="16"
                                       Margin="-4,8,8,8" />
            </Grid>
        </Grid>

        <Grid x:Name="ReactRoot"
              SizeChanged="Button_SizeChanged"
              Margin="4,0,0,0"
              Grid.Column="2">
            <Border Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource SettingsItemBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="24" />
            <Border x:Name="ReactActive"
                    Background="#FFE8AB02"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    CornerRadius="24"
                    Visibility="Collapsed" />
            <controls:AnimatedImage x:Name="ReactionParticles" />
            <Popup x:Name="ReactionPopup"
                   IsHitTestVisible="False"
                   Margin="-48" />

            <controls:GlyphButton x:Name="ReactButton"
                                  Click="DeleteButton_Click"
                                  ContextRequested="ReactButton_ContextRequested"
                                  Glyph="&#xEA87;"
                                  FontSize="16"
                                  CornerRadius="20" />
            <controls:BadgeControl x:Name="ReactCount"
                                   IsHitTestVisible="False"
                                   Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Top"
                                   Visibility="Collapsed" />
        </Grid>
    </Grid>
</UserControl>
