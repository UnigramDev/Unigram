<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:Telegram.Controls.Messages.Content"
                    xmlns:controls="using:Telegram.Controls"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d"
                    d:DesignHeight="300"
                    d:DesignWidth="400">

    <Style TargetType="local:ChecklistContent">
        <Setter Property="MinWidth"
                Value="220" />
        <Setter Property="IsTabStop"
                Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ChecklistContent">
                    <StackPanel>
                        <Grid Margin="10,0,10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Top">
                                <controls:FormattedTextBlock x:Name="TitleText"
                                                             TextWrapping="Wrap"
                                                             TextTrimming="CharacterEllipsis"
                                                             FontWeight="SemiBold"
                                                             FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                                             IsTextSelectionEnabled="False"
                                                             AutoFontSize="False" />
                                <TextBlock x:Name="Type"
                                           Foreground="{ThemeResource MessageSubtleForegroundBrush}"
                                           Style="{StaticResource BodyTextBlockStyle}"
                                           VerticalAlignment="Top"
                                           Margin="0,0,4,4" />
                            </StackPanel>
                        </Grid>

                        <StackPanel x:Name="Tasks" />

                        <TextBlock x:Name="Completed"
                                   Foreground="{ThemeResource MessageSubtleForegroundBrush}"
                                   TextAlignment="Center"
                                   Margin="0,8,0,4" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
