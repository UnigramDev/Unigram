<UserControl x:Class="Telegram.Controls.Cells.LiveStoryMessageCell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Telegram.Controls"
             xmlns:local="using:Telegram.Controls.Cells"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid x:Name="RootGrid"
          Background="Transparent"
          CornerRadius="16">
        <Border x:Name="Expiration" />
        <Grid Padding="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <controls:ProfilePicture x:Name="Photo"
                                     VerticalAlignment="Top"
                                     Size="24" />
            <local:LiveStoryMessagePanel Margin="8,1,4,2"
                                         Grid.Column="1">
                <controls:FormattedTextBlock x:Name="Text"
                                             IsTextSelectionEnabled="False"
                                             TextTrimming="CharacterEllipsis"
                                             TextWrapping="Wrap"
                                             AutoFontSize="False" />

                <Grid x:Name="ReactionRoot">
                    <Border x:Name="StarCountRoot"
                            MinWidth="16"
                            Height="16"
                            CornerRadius="8"
                            VerticalAlignment="Bottom"
                            Margin="0,2,0,0">
                        <TextBlock x:Name="StarCount"
                                   Foreground="#FFFFFF"
                                   Text="* 1"
                                   TextLineBounds="Tight"
                                   TextAlignment="Center"
                                   OpticalMarginAlignment="TrimSideBearings"
                                   FontSize="11"
                                   Padding="4,0,4,0"
                                   VerticalAlignment="Center"
                                   Typography.NumeralAlignment="Tabular"
                                   FontFamily="{ThemeResource EmojiThemeFontFamilyWithSymbols}"/>
                    </Border>
                </Grid>
            </local:LiveStoryMessagePanel>
        </Grid>
    </Grid>
</UserControl>
