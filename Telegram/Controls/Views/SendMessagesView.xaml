<UserControl x:Class="Telegram.Controls.Views.SendMessagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Telegram.Controls.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Telegram.Controls"
             xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:AnimatedImage x:Name="Animated"
                                Source="ms-appx:///Assets/Animations/SendMessagesProgress.tgs"
                                IsCachingEnabled="False"
                                AutoPlay="True"
                                FrameSize="140,140"
                                DecodeFrameType="Logical"
                                CleanOnSourceChanged="False"
                                Width="140"
                                Height="140" />

        <muxc:ProgressBar x:Name="Progress"
                          IsIndeterminate="True"
                          Margin="0,24"
                          Grid.Row="1" />
    </Grid>
</UserControl>
