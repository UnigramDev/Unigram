<controls:ContentPopup x:Class="Telegram.Views.Popups.FrozenPopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:common="using:Telegram.Common"
                       xmlns:controls="using:Telegram.Controls"
                       mc:Ignorable="d"
                       Padding="24,12,24,24">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <controls:AnimatedImage Source="ms-appx:///Assets/Animations/BlockedChats.tgs"
                                IsCachingEnabled="False"
                                AutoPlay="True"
                                FrameSize="140,140"
                                DecodeFrameType="Logical"
                                Width="140"
                                Height="140"
                                Margin="0,0,0,12" />

        <TextBlock Text="{CustomResource AccountFrozenTitle}"
                   Style="{StaticResource PopupTextBlockStyle}"
                   TextAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,0,0,8"
                   Grid.Row="1" />

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <controls:BadgeButton Content="{CustomResource AccountFrozen1Title}"
                                  Badge="{CustomResource AccountFrozen1Text}"
                                  Style="{StaticResource GlyphSubbadgeBadgeButtonPopupStyle}"
                                  Glyph="&#xE98B;"
                                  Margin="-12,0,-12,0"
                                  IsHitTestVisible="False" />
            <controls:BadgeButton Content="{CustomResource AccountFrozen2Title}"
                                  Badge="{CustomResource AccountFrozen2Text}"
                                  Style="{StaticResource GlyphSubbadgeBadgeButtonPopupStyle}"
                                  Glyph="&#xEAA3;"
                                  Margin="-12,-4,-12,0"
                                  IsHitTestVisible="False"
                                  Grid.Row="1" />
            <controls:BadgeButton Content="{CustomResource AccountFrozen3Title}"
                                  Badge="{CustomResource AccountFrozen3Text}"
                                  Style="{StaticResource GlyphSubbadgeBadgeButtonPopupStyle}"
                                  Glyph="&#xEAA8;"
                                  Margin="-12,-4,-12,0"
                                  IsHitTestVisible="False"
                                  Grid.Row="2" />
        </Grid>

    </Grid>
</controls:ContentPopup>
