<controls:ContentPopup x:Class="Telegram.Views.Popups.TranslatePopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:controls="using:Telegram.Controls"
                       xmlns:messages="using:Telegram.Controls.Messages"
                       mc:Ignorable="d"
                       Padding="0,12,0,0">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource InfoBodyTextBlockStyle}"
                   Margin="24,0,24,0">
            <Run x:Name="FromLanguage" />
            <Hyperlink Click="ToLanguage_Click"
                       Foreground="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}">
                <Run x:Name="ToLanguage" />
            </Hyperlink>
        </TextBlock>

        <ScrollViewer x:Name="ScrollingHost"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto"
                      Grid.Row="1">
            <messages:MessageTextBlock x:Name="Block"
                                       Margin="24,12,24,24" />
        </ScrollViewer>

        <controls:ScrollViewerScrim ScrollingHost="{x:Bind ScrollingHost}"
                                    Background="{ThemeResource ContentDialogTopOverlaySolid}"
                                    BottomInset="0"
                                    Grid.Row="1" />
    </Grid>
</controls:ContentPopup>
