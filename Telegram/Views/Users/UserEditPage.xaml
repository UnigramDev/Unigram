<local:HostedPage x:Class="Telegram.Views.Users.UserEditPage"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:controls="using:Telegram.Controls"
                  xmlns:drawers="using:Telegram.Controls.Drawers"
                  xmlns:messages="using:Telegram.Controls.Messages"
                  xmlns:local="using:Telegram.Views"
                  mc:Ignorable="d"
                  Title="{x:Bind ViewModel.Title, Mode=OneWay}">

    <local:HostedPage.Action>
        <Button Content="{CustomResource Save}"
                Command="{x:Bind ViewModel.SendCommand}"
                Style="{StaticResource AccentButtonStyle}" />
    </local:HostedPage.Action>

    <Grid Background="{ThemeResource SettingsPageBackground}">
        <ScrollViewer x:Name="ScrollingHost"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto">
            <controls:SettingsPanel>
                <controls:HeaderedControl x:Name="LayoutRoot"
                                          Footer="{CustomResource RegisterText2}">
                    <Grid Background="{ThemeResource SettingsItemBackground}"
                          BorderBrush="{ThemeResource SettingsItemBorderBrush}"
                          Padding="12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid Height="96"
                              Width="96"
                              Margin="0,0,12,0"
                              Grid.RowSpan="2">
                            <controls:ProfilePicture x:Name="Photo"
                                                     Size="96" />
                        </Grid>

                        <TextBox x:Name="FirstName"
                                 Text="{x:Bind ViewModel.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="{CustomResource FirstNameSmall}"
                                 FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                 MaxLength="{StaticResource UserFirstNameMaxLength}"
                                 VerticalAlignment="Bottom"
                                 Margin="0,0,0,2"
                                 Grid.Column="1" />
                        <TextBox x:Name="LastName"
                                 x:Load="False"
                                 Text="{x:Bind ViewModel.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="{CustomResource LastNameSmall}"
                                 FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                 MaxLength="{StaticResource UserLastNameMaxLength}"
                                 VerticalAlignment="Top"
                                 Margin="0,2,0,0"
                                 Grid.Column="1"
                                 Grid.Row="1" />
                    </Grid>
                    <controls:SettingsButton x:Name="BotPhoto"
                                             x:Load="False"
                                             Content="{CustomResource ChatSetNewPhoto}"
                                             Glyph="&#xE9FE;">
                        <Button.Flyout>
                            <MenuFlyout Placement="BottomEdgeAlignedLeft">
                                <MenuFlyoutItem Text="{CustomResource ChoosePhotoOrVideo}"
                                                Click="{x:Bind ViewModel.SetPhoto}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xEB9F;"
                                                  Style="{StaticResource DefaultFontIconStyle}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="{CustomResource StickersName}"
                                                Click="{x:Bind ViewModel.CreatePhoto}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xF4AA;"
                                                  Style="{StaticResource DefaultFontIconStyle}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Button.Flyout>
                    </controls:SettingsButton>
                    <TextBox x:Name="About"
                             x:Load="False"
                             Text="{x:Bind ViewModel.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             FontFamily="{ThemeResource EmojiThemeFontFamily}"
                             PlaceholderText="{CustomResource DescriptionOptionalPlaceholder}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Padding="12,11,6,12" />
                </controls:HeaderedControl>

                <controls:HeaderedControl x:Name="UsernamePanel"
                                          x:Load="False">
                    <controls:SettingsButton x:Name="Username"
                                             Click="{x:Bind ViewModel.ChangeUsername}"
                                             Content="{CustomResource Username}"
                                             Glyph="&#xE9F8;" />
                    <controls:SettingsButton x:Name="AffiliateProgram"
                                             Content="{CustomResource AffiliateProgramBot}"
                                             Click="{x:Bind ViewModel.OpenAffiliate}"
                                             Glyph="&#xEAA6;" />
                    <controls:SettingsButton x:Name="Stars"
                                             Click="{x:Bind ViewModel.ShowBalance}"
                                             Content="{CustomResource StarsBalance}"
                                             Glyph="&#xEA8D;">
                        <controls:SettingsButton.Badge>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <controls:PremiumStarCount Margin="0,-4,0,-6" />
                                <TextBlock Text="{x:Bind ConvertStarCount(ViewModel.StarCount), Mode=OneWay}"
                                           Grid.Column="1" />
                            </Grid>
                        </controls:SettingsButton.Badge>
                    </controls:SettingsButton>
                </controls:HeaderedControl>

                <controls:HeaderedControl x:Name="BotPanel2"
                                          x:Load="False">
                    <controls:SettingsButton Click="{x:Bind ViewModel.VerifyAccounts}"
                                             Content="{CustomResource BotVerifyAccounts}"
                                             Glyph="&#xEA8B;" />
                </controls:HeaderedControl>

                <controls:HeaderedControl x:Name="BotPanel"
                                          x:Load="False"
                                          Footer="{CustomResource BotManageInfo}">
                    <controls:SettingsButton Click="{x:Bind ViewModel.EditDescription}"
                                             Content="{CustomResource BotEditIntro}"
                                             Glyph="&#xEA0A;" />
                    <controls:SettingsButton Click="{x:Bind ViewModel.EditCommands}"
                                             Content="{CustomResource BotEditCommands}"
                                             Glyph="&#xEA0B;" />
                    <controls:SettingsButton Click="{x:Bind ViewModel.EditSettings}"
                                             Content="{CustomResource BotChangeSettings}"
                                             Glyph="&#xE99A;" />
                </controls:HeaderedControl>

                <controls:HeaderedControl x:Name="NotePanel"
                                          x:Load="False"
                                          Footer="{CustomResource AddNotesInfo}">
                    <Grid>
                        <FlyoutBase.AttachedFlyout>
                            <Flyout x:Name="EmojiFlyout"
                                    Placement="Top"
                                    ShowMode="Transient">
                                <Flyout.FlyoutPresenterStyle>
                                    <Style TargetType="FlyoutPresenter"
                                           BasedOn="{StaticResource CommandFlyoutPresenterStyle}">
                                        <Setter Property="Padding"
                                                Value="0" />
                                    </Style>
                                </Flyout.FlyoutPresenterStyle>
                                <drawers:EmojiDrawer x:Name="EmojiPanel"
                                                     ItemClick="Emoji_ItemClick"
                                                     Width="300"
                                                     Height="360" />
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                        <controls:CaptionTextBox x:Name="NoteField"
                                                 TextChanged="NoteField_TextChanged"
                                                 PlaceholderText="{CustomResource AddNotes}"
                                                 FontFamily="{ThemeResource EmojiThemeFontFamily}"
                                                 Padding="58,11,6,12"
                                                 Height="44"
                                                 MaxLength="12"
                                                 AcceptsReturn="False" />

                        <messages:CustomEmojiCanvas x:Name="CustomEmoji"
                                                    IsHitTestVisible="False"
                                                    Margin="58,10,0,0" />

                        <controls:GlyphButton Click="Emoji_Click"
                                              Glyph="&#xE76E;"
                                              AutomationProperties.Name="{CustomResource Emoji}"
                                              AllowFocusOnInteraction="False"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Bottom"
                                              FontSize="24"
                                              Height="44"
                                              Grid.Column="1" />

                        <TextBlock Text="&#xEADF;"
                                   FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="20"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Margin="20,0,0,0" />
                    </Grid>
                </controls:HeaderedControl>

                <controls:HeaderedControl x:Name="SharePhonePanel"
                                          x:Load="False">
                    <CheckBox x:Name="SharePhoneCheck"
                              IsChecked="{x:Bind ViewModel.SharePhoneNumber, Mode=TwoWay}"
                              Style="{StaticResource SettingsCheckBoxStyle}" />
                </controls:HeaderedControl>

                <controls:HeaderedControl x:Name="PhotoPanel"
                                          x:Load="False">
                    <controls:SettingsButton x:Name="SuggestBirthday"
                                             Click="{x:Bind ViewModel.SuggestBirthday}"
                                             Content="{CustomResource UserSuggestBirthday}"
                                             Glyph="&#xEAE0;"
                                             Visibility="Collapsed" />
                    <controls:SettingsButton x:Name="SuggestPhoto"
                                             Glyph="&#xE9FE;"
                                             Visibility="Collapsed">
                        <Button.Flyout>
                            <MenuFlyout Placement="BottomEdgeAlignedLeft">
                                <MenuFlyoutItem Text="{CustomResource ChoosePhotoOrVideo}"
                                                Click="{x:Bind ViewModel.SetPhoto}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xEB9F;"
                                                  Style="{StaticResource DefaultFontIconStyle}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="{CustomResource UseEmoji}"
                                                Click="{x:Bind ViewModel.CreatePhoto}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE76E;"
                                                  Style="{StaticResource DefaultFontIconStyle}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Button.Flyout>
                    </controls:SettingsButton>
                    <controls:SettingsButton x:Name="PersonalPhoto"
                                             Glyph="&#xE9FD;">
                        <Button.Flyout>
                            <MenuFlyout Placement="BottomEdgeAlignedLeft">
                                <MenuFlyoutItem Text="{CustomResource ChoosePhotoOrVideo}"
                                                Click="{x:Bind ViewModel.SetPersonalPhoto}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xEB9F;"
                                                  Style="{StaticResource DefaultFontIconStyle}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="{CustomResource UseEmoji}"
                                                Click="{x:Bind ViewModel.CreatePersonalPhoto}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE76E;"
                                                  Style="{StaticResource DefaultFontIconStyle}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Button.Flyout>
                    </controls:SettingsButton>

                    <controls:ProfilePicture x:Name="ResetPhotoPhoto"
                                             Size="28"
                                             HorizontalAlignment="Left"
                                             Margin="16,0,0,-44"
                                             Canvas.ZIndex="1" />

                    <controls:SettingsButton x:Name="ResetPhoto"
                                             Click="{x:Bind ViewModel.ResetPhoto}"
                                             Content="{CustomResource ResetToOriginalPhoto}"
                                             Foreground="{ThemeResource DangerButtonBackground}"
                                             IsGlyphVisible="True" />
                </controls:HeaderedControl>
            </controls:SettingsPanel>
        </ScrollViewer>
    </Grid>
</local:HostedPage>
