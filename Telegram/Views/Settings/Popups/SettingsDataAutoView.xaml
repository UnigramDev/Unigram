<UserControl x:Class="Telegram.Views.Settings.Popups.SettingsDataAutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <CheckBox IsChecked="{x:Bind ViewModel.Contacts, Mode=TwoWay}"
                      Content="{CustomResource AutodownloadContacts}"
                      Style="{StaticResource SettingsCheckBoxStyle}"
                      BorderThickness="0,0,0,1" />

            <CheckBox IsChecked="{x:Bind ViewModel.PrivateChats, Mode=TwoWay}"
                      Content="{CustomResource AutodownloadPrivateChats}"
                      Style="{StaticResource SettingsCheckBoxStyle}"
                      BorderThickness="0,0,0,1" />

            <CheckBox IsChecked="{x:Bind ViewModel.Groups, Mode=TwoWay}"
                      Content="{CustomResource AutodownloadGroupChats}"
                      Style="{StaticResource SettingsCheckBoxStyle}"
                      BorderThickness="0,0,0,1" />

            <CheckBox IsChecked="{x:Bind ViewModel.Channels, Mode=TwoWay}"
                      Content="{CustomResource AutodownloadChannels}"
                      Style="{StaticResource SettingsCheckBoxStyle}"
                      CornerRadius="{x:Bind ConvertCornerRadius(ViewModel.IsLimitSupported)}"/>

            <Grid Visibility="{x:Bind ViewModel.IsLimitSupported, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                  Background="{ThemeResource SettingsItemBackground}"
                  BorderBrush="{ThemeResource SettingsItemBorderBrush}"
                  BorderThickness="0,1,0,0"
                  CornerRadius="0,0,4,4"
                  MinHeight="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="208" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{CustomResource AutoDownloadMaxFileSize}"
                           FontFamily="{ThemeResource EmojiThemeFontFamily}"
                           VerticalAlignment="Center"
                           Grid.Column="1"/>

                <Slider Value="{x:Bind ConvertLimit(ViewModel.Limit), Mode=TwoWay, BindBack=ConvertLimitBack}"
                        Minimum="0"
                        Maximum="1"
                        SmallChange="0.01"
                        LargeChange="0.1"
                        StepFrequency="0.01"
                        TickFrequency="0.25"
                        TickPlacement="Outside"
                        IsThumbToolTipEnabled="False"
                        Margin="12,6,16,6"
                        Grid.Column="3"/>

                <TextBlock Text="{x:Bind ConvertUpTo(ViewModel.Limit), Mode=OneWay}"
                           Style="{StaticResource InfoCaptionTextBlockStyle}"
                           VerticalAlignment="Center"
                           Grid.Column="2"/>
            </Grid>

            <StackPanel Visibility="{x:Bind ViewModel.IsPreloadSupported, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                <CheckBox IsChecked="{x:Bind ViewModel.Preload, Mode=TwoWay}"
                          Content="{CustomResource AutoDownloadPreloadVideo}"
                          Margin="0,8,0,0" />

                <TextBlock Text="{x:Bind ConvertPreload(ViewModel.Limit), Mode=OneWay}"
                           Style="{StaticResource InfoCaptionTextBlockStyle}"
                           Margin="0,4,0,0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
