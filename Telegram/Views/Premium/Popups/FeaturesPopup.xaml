<controls:ContentPopup x:Class="Telegram.Views.Premium.Popups.FeaturesPopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
                       xmlns:controls="using:Telegram.Controls"
                       xmlns:cells="using:Telegram.Controls.Cells.Premium"
                       xmlns:selectors="using:Telegram.Selectors"
                       mc:Ignorable="d"
                       Padding="0"
                       IsDismissButtonVisible="True"
                       PrimaryButtonStyle="{StaticResource PremiumButtonStyle}"
                       ButtonsLayout="Vertical">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="440" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top"
              HorizontalAlignment="Stretch"
              Height="320"
              CornerRadius="8,8,0,0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="1,0"
                                     EndPoint="0,1">
                    <GradientStop Color="#FFE75ACC" />
                    <GradientStop Color="#FF6797FF"
                                  Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>

        <FlipView x:Name="ScrollingHost"
                  SelectionChanged="OnSelectionChanged"
                  Background="Transparent">
            <FlipView.ItemTemplateSelector>
                <selectors:PremiumFeatureTemplateSelector>
                    <selectors:PremiumFeatureTemplateSelector.ItemTemplate>
                        <DataTemplate>
                            <cells:PremiumFeatureCell DataContextChanged="OnDataContextChanged" />
                        </DataTemplate>
                    </selectors:PremiumFeatureTemplateSelector.ItemTemplate>
                    <selectors:PremiumFeatureTemplateSelector.StickersTemplate>
                        <DataTemplate>
                            <cells:PremiumFeatureUniqueStickersCell DataContextChanged="OnDataContextChanged" />
                        </DataTemplate>
                    </selectors:PremiumFeatureTemplateSelector.StickersTemplate>
                    <selectors:PremiumFeatureTemplateSelector.LimitsTemplate>
                        <DataTemplate>
                            <cells:PremiumFeatureIncreasedLimitsCell DataContextChanged="OnDataContextChanged" />
                        </DataTemplate>
                    </selectors:PremiumFeatureTemplateSelector.LimitsTemplate>
                    <selectors:PremiumFeatureTemplateSelector.StoriesTemplate>
                        <DataTemplate>
                            <cells:PremiumFeatureUpgradedStoriesCell DataContextChanged="OnDataContextChanged" />
                        </DataTemplate>
                    </selectors:PremiumFeatureTemplateSelector.StoriesTemplate>
                    <selectors:PremiumFeatureTemplateSelector.BusinessTemplate>
                        <DataTemplate>
                            <cells:PremiumFeatureBusinessCell DataContextChanged="OnDataContextChanged" />
                        </DataTemplate>
                    </selectors:PremiumFeatureTemplateSelector.BusinessTemplate>
                </selectors:PremiumFeatureTemplateSelector>
            </FlipView.ItemTemplateSelector>
        </FlipView>

        <muxc:PipsPager x:Name="Pager"
                        SelectedPageIndex="{x:Bind ScrollingHost.SelectedIndex, Mode=TwoWay}"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,12"
                        Grid.Row="1" />
    </Grid>
</controls:ContentPopup>
